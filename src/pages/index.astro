---
import Base from '../layouts/Base.astro';
---

<Base
  title="Grupo Castañeda — Outsourcing Contable Integral en Lima"
  description="Contabilidad, planillas, tesorería y tributación integradas. Tu empresa siempre al día, con información real para tomar decisiones. Lima, Perú."
>

  <!-- ══════════════════════════════════════════
       1. HERO — Qué hacemos, para quién, CTA
  ══════════════════════════════════════════ -->
  <section class="hero">
    <div class="container hero__inner">
      <div class="hero__content">
        <span class="eyebrow" data-reveal>Outsourcing Contable Integral · Lima, Perú</span>

        <h1 class="hero__heading" data-reveal>
          ¿Tu contador solo<br>
          <em>aparece a fin de mes?</em>
        </h1>

        <p class="hero__sub" data-reveal>
          Un contador aparece a fin de mes. Nosotros somos tu área contable completa
          —contabilidad, planillas, tesorería y tributación, en un solo equipo— que se
          anticipa a los problemas y te da con qué decidir.
        </p>

        <div class="hero__actions" data-reveal>
          <a href="/conversemos" class="btn btn--primary">
            Conversemos
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true">
              <path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </a>
          <a href="/servicios" class="btn btn--outline hero__btn-outline">Ver servicios</a>
        </div>
      </div>

      <div class="hero__media" data-reveal>
        <div class="img-slot img-slot--portrait img-slot--on-dark" data-img="hero-principal">
          <span class="img-slot__label">Imagen principal</span>
          <span class="img-slot__hint">Equipo u oficina · ~680 × 880 px</span>
        </div>
      </div>
    </div>
    <div class="hero__rule"></div>
  </section>

  <!-- ══════════════════════════════════════════
       2. STATS BAR
  ══════════════════════════════════════════ -->
  <section class="stats" aria-label="Cifras clave">
    <div class="container stats__inner" data-stagger>
      <div class="stats__item">
        <span class="stats__num" data-counter="10" data-suffix="+">0</span>
        <span class="stats__label">Años de experiencia</span>
      </div>
      <div class="stats__divider" aria-hidden="true"></div>
      <div class="stats__item">
        <span class="stats__num" data-counter="150" data-suffix="+">0</span>
        <span class="stats__label">Empresas atendidas</span>
      </div>
      <div class="stats__divider" aria-hidden="true"></div>
      <div class="stats__item">
        <span class="stats__num" data-counter="4">0</span>
        <span class="stats__label">Áreas integradas</span>
      </div>
      <div class="stats__divider" aria-hidden="true"></div>
      <div class="stats__item">
        <span class="stats__num" data-counter="98" data-suffix="%">0</span>
        <span class="stats__label">Satisfacción de clientes</span>
      </div>
    </div>
  </section>

  <!-- ══════════════════════════════════════════
       2a. VIDEO — Un servicio integral de calidad
  ══════════════════════════════════════════ -->
  <section class="videoint" aria-labelledby="videoint-heading">
    <div class="container">
      <div class="videoint__head" data-reveal>
        <span class="eyebrow">Conócenos</span>
        <h2 class="videoint__title" id="videoint-heading">Un servicio integral de calidad</h2>
      </div>
      <div class="videoint__media" data-reveal>
        <div class="img-slot img-slot--video" data-img="video-servicios">
          <span class="videoint__play" aria-hidden="true">
            <svg width="26" height="26" viewBox="0 0 24 24" fill="none"><path d="M8 5v14l11-7z" fill="currentColor"/></svg>
          </span>
          <span class="img-slot__label">Video — Servicios contables</span>
          <span class="img-slot__hint">16:9 · reemplazar por el video</span>
        </div>
      </div>
    </div>
  </section>

  <!-- ══════════════════════════════════════════
       2b. SECTORES — Prueba social: a quiénes atendemos
  ══════════════════════════════════════════ -->
  <section class="sectors" aria-labelledby="sectors-heading">
    <div class="container">

      <div class="sectors__header" data-reveal>
        <span class="eyebrow">Empresas que confían</span>
        <h2 class="sectors__title" id="sectors-heading">
          Acompañamos empresas<br>de todos los rubros en Lima.
        </h2>
      </div>

      <div class="sectors__grid" data-stagger>

        <div class="sector">
          <span class="sector__icon" aria-hidden="true">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none">
              <path d="M6 7h12l1 13H5L6 7z" stroke="currentColor" stroke-width="1.2" stroke-linejoin="round"/>
              <path d="M9 9V6a3 3 0 016 0v3" stroke="currentColor" stroke-width="1.2" stroke-linecap="round"/>
            </svg>
          </span>
          <span class="sector__label">Comercio y retail</span>
        </div>

        <div class="sector">
          <span class="sector__icon" aria-hidden="true">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none">
              <circle cx="12" cy="12" r="3.2" stroke="currentColor" stroke-width="1.2"/>
              <path d="M12 3v3M12 18v3M3 12h3M18 12h3M5.5 5.5l2 2M16.5 16.5l2 2M18.5 5.5l-2 2M7.5 16.5l-2 2" stroke="currentColor" stroke-width="1.2" stroke-linecap="round"/>
            </svg>
          </span>
          <span class="sector__label">Servicios</span>
        </div>

        <div class="sector">
          <span class="sector__icon" aria-hidden="true">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none">
              <path d="M2 6h11v10H2zM13 9h4l3 3.5V16h-7z" stroke="currentColor" stroke-width="1.2" stroke-linejoin="round"/>
              <circle cx="6.5" cy="18" r="1.6" stroke="currentColor" stroke-width="1.2"/>
              <circle cx="16.5" cy="18" r="1.6" stroke="currentColor" stroke-width="1.2"/>
            </svg>
          </span>
          <span class="sector__label">Logística</span>
        </div>

        <div class="sector">
          <span class="sector__icon" aria-hidden="true">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none">
              <rect x="4" y="3" width="16" height="18" rx="1" stroke="currentColor" stroke-width="1.2"/>
              <path d="M9 21v-4h6v4M8 7h2M14 7h2M8 11h2M14 11h2" stroke="currentColor" stroke-width="1.2" stroke-linecap="round"/>
            </svg>
          </span>
          <span class="sector__label">Construcción</span>
        </div>

        <div class="sector">
          <span class="sector__icon" aria-hidden="true">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none">
              <path d="M12 21S4 14.5 4 9a4 4 0 018-1 4 4 0 018 1c0 5.5-8 12-8 12z" stroke="currentColor" stroke-width="1.2" stroke-linejoin="round"/>
            </svg>
          </span>
          <span class="sector__label">Salud</span>
        </div>

        <div class="sector">
          <span class="sector__icon" aria-hidden="true">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none">
              <rect x="3" y="4" width="18" height="12" rx="1" stroke="currentColor" stroke-width="1.2"/>
              <path d="M9 20h6M12 16v4" stroke="currentColor" stroke-width="1.2" stroke-linecap="round"/>
            </svg>
          </span>
          <span class="sector__label">Tecnología</span>
        </div>

        <div class="sector">
          <span class="sector__icon" aria-hidden="true">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none">
              <circle cx="12" cy="12" r="9" stroke="currentColor" stroke-width="1.2"/>
              <path d="M3 12h18M12 3c3 3 3 15 0 18M12 3c-3 3-3 15 0 18" stroke="currentColor" stroke-width="1.2"/>
            </svg>
          </span>
          <span class="sector__label">Importación</span>
        </div>

        <div class="sector">
          <span class="sector__icon" aria-hidden="true">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none">
              <circle cx="9" cy="8" r="3" stroke="currentColor" stroke-width="1.2"/>
              <path d="M3 20c0-3.3 2.7-6 6-6s6 2.7 6 6" stroke="currentColor" stroke-width="1.2" stroke-linecap="round"/>
              <path d="M16 5.2a3 3 0 010 5.6M21 20c0-2.5-1.5-4.6-3.7-5.5" stroke="currentColor" stroke-width="1.2" stroke-linecap="round"/>
            </svg>
          </span>
          <span class="sector__label">Consultoría</span>
        </div>

      </div>

      <div class="logos" aria-label="Logos de clientes (próximamente)" data-reveal>
        <div class="logos__track" aria-hidden="true">
          <span class="logo-ph">?</span><span class="logo-ph">?</span><span class="logo-ph">?</span><span class="logo-ph">?</span><span class="logo-ph">?</span><span class="logo-ph">?</span><span class="logo-ph">?</span><span class="logo-ph">?</span><span class="logo-ph">?</span><span class="logo-ph">?</span><span class="logo-ph">?</span><span class="logo-ph">?</span><span class="logo-ph">?</span><span class="logo-ph">?</span><span class="logo-ph">?</span><span class="logo-ph">?</span>
        </div>
      </div>

    </div>
  </section>

  <!-- ══════════════════════════════════════════
       3. EL PROBLEMA — Resonancia con el cliente
  ══════════════════════════════════════════ -->
  <section class="section problem" aria-labelledby="problem-heading">
    <div class="container">

      <div class="section-header" data-reveal>
        <span class="eyebrow">¿Te identificas?</span>
        <h2 class="section-title" id="problem-heading">
          La contabilidad tradicional<br>ya no es suficiente.
        </h2>
        <div class="gold-line"></div>
      </div>

      <div class="problem__grid" data-stagger>

        <div class="problem-card">
          <div class="problem-card__icon" aria-hidden="true">
            <svg width="28" height="28" viewBox="0 0 28 28" fill="none">
              <circle cx="14" cy="14" r="12" stroke="currentColor" stroke-width="1.2"/>
              <path d="M14 8v7" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
              <circle cx="14" cy="19" r="1" fill="currentColor"/>
            </svg>
          </div>
          <h3 class="problem-card__label">No es solo declarar a fin de mes</h3>
          <p class="problem-card__text">
            Solo recibes declaraciones una vez al mes. Nunca un análisis,
            nunca una alerta, nunca un "ojo con esto".
          </p>
        </div>

        <div class="problem-card problem-card--center">
          <div class="problem-card__icon" aria-hidden="true">
            <svg width="28" height="28" viewBox="0 0 28 28" fill="none">
              <path d="M14 3L3 9v7c0 6.5 4.8 11.1 11 12.3C20.2 27.1 25 22.5 25 16V9L14 3z" stroke="currentColor" stroke-width="1.2"/>
              <path d="M10 14l3 3 6-6" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <h3 class="problem-card__label">Un verdadero control</h3>
          <p class="problem-card__text">
            No sabes realmente si tu empresa está bien. Tomas decisiones
            de inversión, contratación o crédito sin información confiable.
          </p>
        </div>

        <div class="problem-card">
          <div class="problem-card__icon" aria-hidden="true">
            <svg width="28" height="28" viewBox="0 0 28 28" fill="none">
              <path d="M14 3C8 3 3 8 3 14s5 11 11 11 11-5 11-11S20 3 14 3z" stroke="currentColor" stroke-width="1.2"/>
              <path d="M10 10c0-2.2 1.8-4 4-4s4 1.8 4 4c0 2-1.5 3.5-3.5 4v2" stroke="currentColor" stroke-width="1.2" stroke-linecap="round"/>
              <circle cx="14.5" cy="22" r="1" fill="currentColor"/>
            </svg>
          </div>
          <h3 class="problem-card__label">El aliado para el éxito</h3>
          <p class="problem-card__text">
            Mereces un equipo contable que trabaje para tu empresa,
            no solo para cumplir con SUNAT.
          </p>
        </div>

      </div>
    </div>
  </section>

  <!-- ══════════════════════════════════════════
       4. LAS CONSECUENCIAS — Las apuestas
  ══════════════════════════════════════════ -->
  <section class="stakes" aria-labelledby="stakes-heading">
    <div class="container stakes__inner">

      <div class="stakes__text" data-reveal-left>
        <span class="eyebrow stakes__eyebrow">Lo que está en juego</span>
        <h2 class="stakes__heading" id="stakes-heading">
          Cada mes sin control real<br>es un mes de riesgo acumulado.
        </h2>
        <p class="stakes__sub">
          No actuar tiene un costo concreto. La mayoría de los problemas
          contables que terminan en multas o contingencias eran previsibles.
        </p>
      </div>

      <div class="stakes__list" data-stagger>
        <div class="stakes__item">
          <svg width="18" height="18" viewBox="0 0 18 18" fill="none" aria-hidden="true">
            <path d="M9 2L2 5.5v4C2 13.1 5.1 16.4 9 17.2c3.9-.8 7-4.1 7-7.7v-4L9 2z" stroke="currentColor" stroke-width="1.2"/>
            <path d="M6 9l2 2 4-4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
          <span>Multas por declaraciones fuera de plazo o incorrectas</span>
        </div>
        <div class="stakes__item">
          <svg width="18" height="18" viewBox="0 0 18 18" fill="none" aria-hidden="true">
            <path d="M9 2L2 5.5v4C2 13.1 5.1 16.4 9 17.2c3.9-.8 7-4.1 7-7.7v-4L9 2z" stroke="currentColor" stroke-width="1.2"/>
            <path d="M6 9l2 2 4-4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
          <span>Crisis de liquidez que llegan sin aviso</span>
        </div>
        <div class="stakes__item">
          <svg width="18" height="18" viewBox="0 0 18 18" fill="none" aria-hidden="true">
            <path d="M9 2L2 5.5v4C2 13.1 5.1 16.4 9 17.2c3.9-.8 7-4.1 7-7.7v-4L9 2z" stroke="currentColor" stroke-width="1.2"/>
            <path d="M6 9l2 2 4-4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
          <span>Decisiones de inversión sin respaldo financiero real</span>
        </div>
        <div class="stakes__item">
          <svg width="18" height="18" viewBox="0 0 18 18" fill="none" aria-hidden="true">
            <path d="M9 2L2 5.5v4C2 13.1 5.1 16.4 9 17.2c3.9-.8 7-4.1 7-7.7v-4L9 2z" stroke="currentColor" stroke-width="1.2"/>
            <path d="M6 9l2 2 4-4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
          <span>Contingencias tributarias que se acumulan silenciosamente</span>
        </div>
        <div class="stakes__item">
          <svg width="18" height="18" viewBox="0 0 18 18" fill="none" aria-hidden="true">
            <path d="M9 2L2 5.5v4C2 13.1 5.1 16.4 9 17.2c3.9-.8 7-4.1 7-7.7v-4L9 2z" stroke="currentColor" stroke-width="1.2"/>
            <path d="M6 9l2 2 4-4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
          <span>Pérdida de tiempo gerencial resolviendo problemas contables</span>
        </div>
        <div class="stakes__item">
          <svg width="18" height="18" viewBox="0 0 18 18" fill="none" aria-hidden="true">
            <path d="M9 2L2 5.5v4C2 13.1 5.1 16.4 9 17.2c3.9-.8 7-4.1 7-7.7v-4L9 2z" stroke="currentColor" stroke-width="1.2"/>
            <path d="M6 9l2 2 4-4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
          <span>Crecer sin saber si el crecimiento es rentable</span>
        </div>
      </div>

    </div>
  </section>

  <!-- ══════════════════════════════════════════
       5. EL GUÍA — Somos la solución (empatía + autoridad)
  ══════════════════════════════════════════ -->
  <section class="section guide" aria-labelledby="guide-heading">
    <div class="container guide__inner">

      <div class="guide__text" data-reveal-left>
        <span class="eyebrow">Por qué Grupo Castañeda</span>
        <h2 class="section-title" id="guide-heading">
          Entendemos lo que sientes.<br>
          <em>Y sabemos cómo resolverlo.</em>
        </h2>
        <div class="gold-line"></div>
        <p>
          Hemos trabajado con más de 150 empresas en Lima. Sabemos que el empresario
          no quiere ser contador — quiere tener la certeza de que su empresa está bien,
          y poder enfocarse en lo que realmente le corresponde: crecer.
        </p>
        <p>
          Por eso no somos un estudio que "entrega declaraciones". Somos el área
          contable de tu empresa: integrada, proactiva, y siempre disponible.
        </p>
        <div class="guide__media" data-reveal>
          <div class="img-slot img-slot--wide" data-img="guide-equipo">
            <span class="img-slot__label">El equipo Grupo Castañeda</span>
            <span class="img-slot__hint">Foto de presentación · ~720 × 380 px</span>
          </div>
        </div>
        <a href="/nosotros" class="btn btn--outline guide__btn">Conoce nuestro equipo</a>
      </div>

      <div class="guide__credentials" data-stagger>

        <div class="credential">
          <span class="credential__num" data-counter="10" data-suffix="+">10+</span>
          <div>
            <strong class="credential__title">Años de experiencia</strong>
            <p class="credential__desc">Acompañando empresas en Lima desde su etapa inicial hasta su consolidación.</p>
          </div>
        </div>

        <div class="credential">
          <span class="credential__num" data-counter="150" data-suffix="+">150+</span>
          <div>
            <strong class="credential__title">Empresas atendidas</strong>
            <p class="credential__desc">De todos los rubros: comercio, servicios, logística, tecnología, construcción y más.</p>
          </div>
        </div>

        <div class="credential">
          <span class="credential__num">4</span>
          <div>
            <strong class="credential__title">Áreas integradas</strong>
            <p class="credential__desc">Contabilidad, planillas, tesorería y tributación bajo un mismo equipo coordinado.</p>
          </div>
        </div>

      </div>

    </div>
  </section>

  <!-- ══════════════════════════════════════════
       6. EL PLAN — 3 pasos simples
  ══════════════════════════════════════════ -->
  <section class="section plan" aria-labelledby="plan-heading">
    <div class="container">

      <div class="section-header section-header--center" data-reveal>
        <span class="eyebrow">Así de simple</span>
        <h2 class="section-title" id="plan-heading">
          Tres pasos para tener<br>el área contable que mereces.
        </h2>
        <div class="gold-line" style="margin-inline: auto;"></div>
      </div>

      <div class="plan__steps" data-stagger>

        <div class="plan__step">
          <div class="plan__step-num" aria-hidden="true">01</div>
          <div class="plan__connector" aria-hidden="true"></div>
          <h3 class="plan__step-title">Conversamos</h3>
          <p class="plan__step-desc">
            Una llamada de 30 minutos para entender tu empresa, tu situación
            actual y lo que necesitas. Sin compromiso, sin tecnicismos.
          </p>
        </div>

        <div class="plan__step">
          <div class="plan__step-num" aria-hidden="true">02</div>
          <div class="plan__connector" aria-hidden="true"></div>
          <h3 class="plan__step-title">Diseñamos tu esquema</h3>
          <p class="plan__step-desc">
            Definimos las áreas que necesitas, hacemos la transición ordenada
            desde tu contador o estudio anterior, y ponemos todo al día.
          </p>
        </div>

        <div class="plan__step">
          <div class="plan__step-num" aria-hidden="true">03</div>
          <h3 class="plan__step-title">Tu empresa, siempre al día</h3>
          <p class="plan__step-desc">
            A partir de ese momento tienes un equipo dedicado, alertas preventivas,
            reportes mensuales y respuesta garantizada en el día.
          </p>
        </div>

      </div>

      <div class="plan__cta" data-reveal>
        <a href="/conversemos" class="btn btn--primary">
          Empezar ahora
          <svg width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true">
            <path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </a>
      </div>

    </div>
  </section>

  <!-- ══════════════════════════════════════════
       7. SERVICIOS — Preview de las 4 áreas
  ══════════════════════════════════════════ -->
  <section class="section services" aria-labelledby="services-heading">
    <div class="container">

      <div class="section-header" data-reveal>
        <span class="eyebrow">Lo que hacemos</span>
        <h2 class="section-title" id="services-heading">Cuatro áreas. Un solo equipo.</h2>
        <div class="gold-line"></div>
        <p class="section-desc">
          Cada área trabaja en coordinación para darte una visión financiera
          completa y preventiva de tu empresa.
        </p>
      </div>

      <div class="services__grid" data-stagger>

        <div class="service-card">
          <div class="service-card__icon" aria-hidden="true">
            <svg width="28" height="28" viewBox="0 0 28 28" fill="none">
              <rect x="4" y="3" width="20" height="23" rx="2" stroke="currentColor" stroke-width="1.2"/>
              <line x1="8" y1="10" x2="20" y2="10" stroke="currentColor" stroke-width="1.2"/>
              <line x1="8" y1="15" x2="20" y2="15" stroke="currentColor" stroke-width="1.2"/>
              <line x1="8" y1="20" x2="15" y2="20" stroke="currentColor" stroke-width="1.2"/>
            </svg>
          </div>
          <h3 class="service-card__title">Contabilidad</h3>
          <p class="service-card__desc">Registro riguroso, estados financieros reales y análisis mensual que te permite decidir con datos.</p>
          <span class="service-card__tag">NIIF / PCGE</span>
        </div>

        <div class="service-card">
          <div class="service-card__icon" aria-hidden="true">
            <svg width="28" height="28" viewBox="0 0 28 28" fill="none">
              <circle cx="14" cy="9" r="5" stroke="currentColor" stroke-width="1.2"/>
              <path d="M5 25c0-4.97 4.03-9 9-9s9 4.03 9 9" stroke="currentColor" stroke-width="1.2" stroke-linecap="round"/>
            </svg>
          </div>
          <h3 class="service-card__title">Planillas</h3>
          <p class="service-card__desc">Cálculo de remuneraciones, AFP/ONP, gratificaciones, CTS y presentación puntual ante SUNAT.</p>
          <span class="service-card__tag">T-Registro / PLM</span>
        </div>

        <div class="service-card">
          <div class="service-card__icon" aria-hidden="true">
            <svg width="28" height="28" viewBox="0 0 28 28" fill="none">
              <rect x="3" y="7" width="22" height="16" rx="2" stroke="currentColor" stroke-width="1.2"/>
              <path d="M3 12h22" stroke="currentColor" stroke-width="1.2"/>
              <circle cx="9" cy="18" r="2" stroke="currentColor" stroke-width="1.2"/>
            </svg>
          </div>
          <h3 class="service-card__title">Tesorería</h3>
          <p class="service-card__desc">Control de flujo de caja, conciliaciones bancarias y alertas preventivas de liquidez.</p>
          <span class="service-card__tag">Flujo de caja</span>
        </div>

        <div class="service-card">
          <div class="service-card__icon" aria-hidden="true">
            <svg width="28" height="28" viewBox="0 0 28 28" fill="none">
              <path d="M14 3L3 8.5v5.5c0 6.4 4.8 11.4 11 12.7C20.2 25.4 25 20.4 25 14v-5.5L14 3z" stroke="currentColor" stroke-width="1.2"/>
              <path d="M10 14l3 3 6-6" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <h3 class="service-card__title">Tributación</h3>
          <p class="service-card__desc">Planificación tributaria, declaraciones, atención de fiscalizaciones y estrategias para reducir la carga fiscal.</p>
          <span class="service-card__tag">SUNAT / PDT</span>
        </div>

      </div>

      <div class="services__more" data-reveal>
        <a href="/servicios" class="btn btn--outline">Ver detalle de servicios</a>
      </div>

    </div>
  </section>

  <!-- ══════════════════════════════════════════
       8. VISIÓN DE ÉXITO — Cómo se ve la vida después
  ══════════════════════════════════════════ -->
  <section class="success" aria-labelledby="success-heading">
    <div class="container success__inner">

      <div class="success__header" data-reveal>
        <span class="eyebrow success__eyebrow">La transformación</span>
        <h2 class="success__heading" id="success-heading">
          Imagina saber exactamente<br>
          <em>cómo está tu empresa.</em>
        </h2>
        <p class="success__sub">
          Esto es lo que tienen nuestros clientes a los 90 días de trabajar con nosotros:
        </p>
      </div>

      <div class="success__media" data-reveal>
        <div class="img-slot img-slot--wide" data-img="reporte-mensual">
          <span class="img-slot__label">Imagen — Reporte ejecutivo mensual</span>
          <span class="img-slot__hint">Captura o diseño · ~1200 × 760 px</span>
        </div>
      </div>

      <div class="success__grid" data-stagger>

        <div class="success__item">
          <div class="success__check" aria-hidden="true">
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
              <path d="M3 8l3.5 3.5L13 4.5" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <p>Un informe mensual que entiendes sin ser contador</p>
        </div>

        <div class="success__item">
          <div class="success__check" aria-hidden="true">
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
              <path d="M3 8l3.5 3.5L13 4.5" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <p>Saber con certeza si tu flujo de caja cubre las obligaciones del mes</p>
        </div>

        <div class="success__item">
          <div class="success__check" aria-hidden="true">
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
              <path d="M3 8l3.5 3.5L13 4.5" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <p>Declaraciones enviadas antes del vencimiento, siempre</p>
        </div>

        <div class="success__item">
          <div class="success__check" aria-hidden="true">
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
              <path d="M3 8l3.5 3.5L13 4.5" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <p>Respuesta a cualquier consulta tributaria antes del cierre del día</p>
        </div>

        <div class="success__item">
          <div class="success__check" aria-hidden="true">
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
              <path d="M3 8l3.5 3.5L13 4.5" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <p>Planilla de colaboradores gestionada sin errores ni retrasos</p>
        </div>

        <div class="success__item">
          <div class="success__check" aria-hidden="true">
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
              <path d="M3 8l3.5 3.5L13 4.5" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <p>Decisiones de inversión tomadas con datos reales, no intuición</p>
        </div>

        <div class="success__item">
          <div class="success__check" aria-hidden="true">
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
              <path d="M3 8l3.5 3.5L13 4.5" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <p>Cero sorpresas en fiscalizaciones: todo ordenado y sustentado</p>
        </div>

        <div class="success__item">
          <div class="success__check" aria-hidden="true">
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
              <path d="M3 8l3.5 3.5L13 4.5" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <p>Tiempo gerencial recuperado para enfocarte en hacer crecer tu empresa</p>
        </div>

      </div>

    </div>
  </section>

  <!-- ══════════════════════════════════════════
       9. BENEFICIOS — Value stack
  ══════════════════════════════════════════ -->
  <section class="section benefits" aria-labelledby="benefits-heading">
    <div class="container">

      <div class="section-header" data-reveal>
        <span class="eyebrow">Lo que incluye</span>
        <h2 class="section-title" id="benefits-heading">
          Todo lo que necesita<br>tu empresa, integrado.
        </h2>
        <div class="gold-line"></div>
      </div>

      <div class="benefits__grid" data-stagger>

        <div class="benefit">
          <div class="benefit__icon" aria-hidden="true">
            <svg width="22" height="22" viewBox="0 0 22 22" fill="none">
              <path d="M4 11l4 4 10-8" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <div>
            <h3 class="benefit__title">Equipo dedicado a tu empresa</h3>
            <p class="benefit__desc">Un contador principal asignado, con nombre y apellido. Siempre sabes a quién escribirle.</p>
          </div>
        </div>

        <div class="benefit">
          <div class="benefit__icon" aria-hidden="true">
            <svg width="22" height="22" viewBox="0 0 22 22" fill="none">
              <path d="M4 11l4 4 10-8" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <div>
            <h3 class="benefit__title">Respuesta garantizada en el día</h3>
            <p class="benefit__desc">Ninguna consulta queda sin respuesta por más de 24 horas hábiles.</p>
          </div>
        </div>

        <div class="benefit">
          <div class="benefit__icon" aria-hidden="true">
            <svg width="22" height="22" viewBox="0 0 22 22" fill="none">
              <path d="M4 11l4 4 10-8" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <div>
            <h3 class="benefit__title">Alertas preventivas</h3>
            <p class="benefit__desc">Te avisamos antes de vencimientos, contingencias y riesgos de liquidez.</p>
          </div>
        </div>

        <div class="benefit">
          <div class="benefit__icon" aria-hidden="true">
            <svg width="22" height="22" viewBox="0 0 22 22" fill="none">
              <path d="M4 11l4 4 10-8" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <div>
            <h3 class="benefit__title">Reportes ejecutivos mensuales</h3>
            <p class="benefit__desc">En lenguaje de negocio, no tecnicismos: estado de resultados, flujo y KPIs clave.</p>
          </div>
        </div>

        <div class="benefit">
          <div class="benefit__icon" aria-hidden="true">
            <svg width="22" height="22" viewBox="0 0 22 22" fill="none">
              <path d="M4 11l4 4 10-8" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <div>
            <h3 class="benefit__title">Fee fijo sin sorpresas</h3>
            <p class="benefit__desc">Un precio mensual que incluye todo. Sin cobros extra por declaraciones adicionales.</p>
          </div>
        </div>

        <div class="benefit">
          <div class="benefit__icon" aria-hidden="true">
            <svg width="22" height="22" viewBox="0 0 22 22" fill="none">
              <path d="M4 11l4 4 10-8" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <div>
            <h3 class="benefit__title">Transición sin fricciones</h3>
            <p class="benefit__desc">Nos encargamos de ordenar el histórico si vienes de otro estudio o contador.</p>
          </div>
        </div>

      </div>
    </div>
  </section>

  <!-- ══════════════════════════════════════════
       10. TESTIMONIOS — Prueba social
  ══════════════════════════════════════════ -->
  <section class="section testimonials" aria-labelledby="testimonials-heading">
    <div class="container">

      <div class="section-header" data-reveal>
        <span class="eyebrow">Lo que dicen nuestros clientes</span>
        <h2 class="section-title" id="testimonials-heading">
          Empresarios que ya<br>tienen claridad.
        </h2>
        <div class="gold-line"></div>
      </div>

      <div class="testimonials__grid" data-stagger>

        <blockquote class="tcard tcard--featured">
          <svg class="tcard__mark" width="28" height="22" viewBox="0 0 48 36" fill="none" aria-hidden="true">
            <path d="M0 36V22.4C0 14.933 2.533 9.067 7.6 4.8 12.667.533 19.2-.533 27.2.8L25.6 6.4C21.333 5.6 17.6 6.267 14.4 8.4 11.2 10.533 9.333 13.6 8.8 17.6H16V36H0zm26 0V22.4c0-7.467 2.533-13.333 7.6-17.6C38.667.533 45.2-.533 53.2.8L51.6 6.4c-4.267-.8-8 .133-11.2 2.8-3.2 2.667-5.067 6.4-5.6 11.2H42V36H26z" fill="currentColor"/>
          </svg>
          <p class="tcard__text">Llevaba tres años con el mismo estudio y nunca supe realmente cómo estaba mi empresa. Con Grupo Castañeda, en el primer mes ya tenía claridad sobre mis costos, mis obligaciones y mi flujo proyectado.</p>
          <footer class="tcard__footer">
            <div class="tcard__avatar" aria-hidden="true">R</div>
            <div>
              <cite class="tcard__name">Ricardo M.</cite>
              <span class="tcard__role">Gerente General · Logística</span>
            </div>
          </footer>
        </blockquote>

        <blockquote class="tcard">
          <p class="tcard__text">Crecimos de 3 a 18 colaboradores en un año. Sin el apoyo en planillas y tesorería, ese crecimiento hubiera sido caótico. Ellos nos ayudaron a escalar sin perder el control financiero.</p>
          <footer class="tcard__footer">
            <div class="tcard__avatar" aria-hidden="true">P</div>
            <div>
              <cite class="tcard__name">Patricia O.</cite>
              <span class="tcard__role">CEO · Servicios digitales</span>
            </div>
          </footer>
        </blockquote>

        <blockquote class="tcard">
          <p class="tcard__text">Lo que más valoro es la comunicación. Antes tardaba días en recibir respuesta. Ahora me responden el mismo día, con claridad y sin hacerme sentir que estoy molestando.</p>
          <footer class="tcard__footer">
            <div class="tcard__avatar" aria-hidden="true">C</div>
            <div>
              <cite class="tcard__name">Claudia V.</cite>
              <span class="tcard__role">Socia Gerente · Consultoría RRHH</span>
            </div>
          </footer>
        </blockquote>

        <blockquote class="tcard">
          <p class="tcard__text">El informe mensual no es un PDF lleno de números. Es una página que me dice cómo estoy, qué debo hacer y qué viene. Eso vale.</p>
          <footer class="tcard__footer">
            <div class="tcard__avatar" aria-hidden="true">M</div>
            <div>
              <cite class="tcard__name">Marco A.</cite>
              <span class="tcard__role">Director · Importadora</span>
            </div>
          </footer>
        </blockquote>

        <blockquote class="tcard">
          <p class="tcard__text">Tenía una deuda con SUNAT que arrastraba hace dos años. El equipo no solo me ayudó a regularizarla, sino que me explicó cómo evitar que vuelva a pasar.</p>
          <footer class="tcard__footer">
            <div class="tcard__avatar" aria-hidden="true">J</div>
            <div>
              <cite class="tcard__name">Juan Carlos H.</cite>
              <span class="tcard__role">Propietario · Retail</span>
            </div>
          </footer>
        </blockquote>

        <blockquote class="tcard">
          <p class="tcard__text">Vinimos porque nuestro contador anterior desapareció con tres meses sin declarar. En menos de dos semanas estábamos al día. La transición fue increíblemente ordenada.</p>
          <footer class="tcard__footer">
            <div class="tcard__avatar" aria-hidden="true">S</div>
            <div>
              <cite class="tcard__name">Sofía R.</cite>
              <span class="tcard__role">Administradora · Salud veterinaria</span>
            </div>
          </footer>
        </blockquote>

      </div>
    </div>
  </section>

  <!-- ══════════════════════════════════════════
       11. CTA FINAL
  ══════════════════════════════════════════ -->
  <section class="cta-final" aria-labelledby="cta-final-heading">
    <div class="container cta-final__inner" data-reveal>
      <div>
        <h2 class="cta-final__heading" id="cta-final-heading">
          El primer paso es<br>una conversación.
        </h2>
        <p class="cta-final__sub">
          Cuéntanos sobre tu empresa. Te respondemos antes de las 24 horas
          y te proponemos, sin compromiso, cómo podemos ayudarte.
        </p>
        <div class="cta-final__actions">
          <a href="/conversemos" class="btn btn--ghost-dark">
            Agenda la llamada
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true">
              <path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </a>
          <a
            href="https://wa.me/51969357784?text=Hola%2C%20me%20gustar%C3%ADa%20conocer%20m%C3%A1s%20sobre%20sus%20servicios%20contables."
            class="btn btn--wa"
            target="_blank"
            rel="noopener noreferrer"
          >
            <svg width="18" height="18" viewBox="0 0 18 18" fill="none" aria-hidden="true">
              <path d="M9 1.5C4.86 1.5 1.5 4.86 1.5 9c0 1.41.38 2.74 1.04 3.89L1.5 16.5l3.74-1.01A7.47 7.47 0 009 16.5c4.14 0 7.5-3.36 7.5-7.5S13.14 1.5 9 1.5z" stroke="currentColor" stroke-width="1.2"/>
              <path d="M6.5 7.5c.5 1.2 1.5 2.8 3.5 3.5" stroke="currentColor" stroke-width="1.2" stroke-linecap="round"/>
            </svg>
            WhatsApp
          </a>
        </div>
      </div>
    </div>
  </section>

</Base>

<style>
  /* ── HERO ─────────────────────────────────── */
  .hero {
    background: var(--color-navy);
    padding-top: calc(64px + var(--space-24));
    padding-bottom: var(--space-20);
    position: relative;
    overflow: hidden;
  }

  .hero::before {
    content: '';
    position: absolute;
    inset: 0;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.04'/%3E%3C/svg%3E");
    background-size: 200px;
    pointer-events: none;
    opacity: 0.6;
  }

  .hero::after {
    content: '';
    position: absolute;
    right: -80px;
    top: 50%;
    transform: translateY(-50%);
    width: 480px;
    height: 480px;
    border: 0.5px solid rgba(200, 154, 60, 0.12);
    border-radius: 50%;
    pointer-events: none;
  }

  .hero__inner {
    display: grid;
    grid-template-columns: 1fr auto;
    gap: var(--space-16);
    align-items: end;
    position: relative;
    z-index: 1;
  }

  .hero__content { max-width: 680px; }

  .hero__content .eyebrow { color: var(--color-gold); }

  .hero__heading {
    font-family: var(--font-serif);
    font-size: clamp(var(--text-3xl), 5.5vw, var(--text-5xl));
    font-weight: 400;
    color: var(--color-white);
    line-height: 1.1;
    letter-spacing: -0.01em;
    margin-top: var(--space-4);
    margin-bottom: var(--space-6);
  }

  .hero__heading em { font-style: italic; color: var(--color-gold); }

  .hero__sub {
    font-size: var(--text-base);
    line-height: 1.7;
    color: rgba(255,255,255,0.65);
    max-width: 520px;
    margin-bottom: var(--space-10);
  }

  .hero__actions { display: flex; gap: var(--space-4); flex-wrap: wrap; }

  .hero__btn-outline {
    background: transparent;
    color: var(--color-white);
    border-color: rgba(255,255,255,0.25);
  }

  .hero__btn-outline:hover {
    background: rgba(255,255,255,0.06);
    border-color: rgba(255,255,255,0.45);
  }

  .hero__badge {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-2);
    border: 0.5px solid rgba(200, 154, 60, 0.3);
    border-radius: var(--radius-md);
    padding: var(--space-8) var(--space-10);
    text-align: center;
    background: rgba(200, 154, 60, 0.06);
    backdrop-filter: blur(4px);
  }

  .hero__badge-num {
    font-family: var(--font-serif);
    font-size: var(--text-4xl);
    color: var(--color-gold);
    line-height: 1;
  }

  .hero__badge-label {
    font-size: var(--text-xs);
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: rgba(255,255,255,0.45);
  }

  .hero__rule {
    margin-top: var(--space-20);
    height: 0.5px;
    background: linear-gradient(to right, transparent, rgba(229,224,214,0.2) 20%, rgba(229,224,214,0.2) 80%, transparent);
  }

  /* ── STATS ────────────────────────────────── */
  .stats { background: var(--color-navy); padding-block: var(--space-12); }

  .stats__inner {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-12);
  }

  .stats__item { display: flex; flex-direction: column; align-items: center; gap: var(--space-2); text-align: center; }

  .stats__num {
    font-family: var(--font-serif);
    font-size: var(--text-3xl);
    font-weight: 400;
    color: var(--color-gold);
    line-height: 1;
  }

  .stats__label {
    font-size: var(--text-xs);
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: rgba(255,255,255,0.4);
    max-width: 120px;
    text-align: center;
  }

  .stats__divider { width: 0.5px; height: 48px; background: rgba(229,224,214,0.15); flex-shrink: 0; }

  /* ── SECTORES ─────────────────────────────── */
  .sectors {
    background: var(--color-cream);
    padding-block: var(--space-20);
    border-bottom: var(--border-thin);
  }

  .sectors__header {
    text-align: center;
    max-width: 560px;
    margin: 0 auto var(--space-12);
  }

  .sectors__title {
    font-family: var(--font-serif);
    font-size: clamp(var(--text-xl), 2.6vw, var(--text-2xl));
    font-weight: 400;
    color: var(--color-navy);
    line-height: 1.25;
    margin-top: var(--space-3);
  }

  .sectors__grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-4);
  }

  .sector {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    padding: var(--space-4) var(--space-5);
    border: 0.5px solid transparent;
    border-radius: var(--radius-md);
    background: var(--color-white);
    box-shadow: var(--shadow-card);
    transition: border-color var(--transition-base), transform var(--transition-base), box-shadow var(--transition-base);
  }

  .sector:hover {
    border-color: var(--color-gold);
    transform: translateY(-2px);
    box-shadow: var(--shadow-card-hover);
  }

  .sector__icon {
    width: 36px;
    height: 36px;
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-gold);
  }

  .sector__label {
    font-size: var(--text-sm);
    color: var(--color-navy);
    letter-spacing: 0.01em;
  }

  @media (max-width: 1024px) {
    .sectors__grid { grid-template-columns: repeat(2, 1fr); }
  }

  @media (max-width: 540px) {
    .sectors__grid { grid-template-columns: 1fr; }
  }

  /* ── VIDEO INTEGRAL ───────────────────────── */
  .videoint { padding-block: var(--space-24); background: var(--color-white); }

  .videoint__head { text-align: center; max-width: 640px; margin: 0 auto var(--space-12); }

  .videoint__title {
    font-family: var(--font-serif);
    font-size: clamp(var(--text-2xl), 3.5vw, var(--text-3xl));
    font-weight: 400;
    color: var(--color-navy);
    line-height: 1.2;
    margin-top: var(--space-3);
  }

  .videoint__media { max-width: 880px; margin: 0 auto; }

  .videoint__play {
    width: 64px;
    height: 64px;
    border-radius: 50%;
    background: var(--color-gold);
    color: var(--color-white);
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: var(--space-3);
  }

  /* ── LOGOS ROTATIVOS (clientes) ───────────── */
  .logos {
    margin-top: var(--space-12);
    overflow: hidden;
    -webkit-mask-image: linear-gradient(90deg, transparent, #000 8%, #000 92%, transparent);
    mask-image: linear-gradient(90deg, transparent, #000 8%, #000 92%, transparent);
  }

  .logos__track {
    display: flex;
    width: max-content;
    animation: logos-marquee 32s linear infinite;
  }

  .logo-ph {
    width: 120px;
    height: 56px;
    flex-shrink: 0;
    margin-right: var(--space-8);
    display: flex;
    align-items: center;
    justify-content: center;
    border: 0.5px solid var(--color-beige);
    border-radius: var(--radius-md);
    background: var(--color-white);
    color: var(--color-muted);
    font-family: var(--font-serif);
    font-size: var(--text-2xl);
  }

  @keyframes logos-marquee {
    from { transform: translateX(0); }
    to   { transform: translateX(-50%); }
  }

  @media (prefers-reduced-motion: reduce) {
    .logos__track { animation: none; }
  }

  /* ── PROBLEMA ─────────────────────────────── */
  .problem__grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-6);
    margin-top: var(--space-4);
  }

  .problem-card {
    border: 0.5px solid transparent;
    border-radius: var(--radius-md);
    padding: var(--space-8);
    background: var(--color-white);
    box-shadow: var(--shadow-card);
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .problem-card--center {
    background: var(--color-navy);
    border-color: rgba(200,154,60,0.3);
  }

  .problem-card--center .problem-card__icon { color: var(--color-gold); }
  .problem-card--center .problem-card__label { color: var(--color-gold); }
  .problem-card--center .problem-card__text { color: rgba(255,255,255,0.75); }

  .problem-card__icon {
    color: var(--color-terra);
    width: 48px;
    height: 48px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .problem-card__label {
    font-size: var(--text-xs);
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--color-terra);
    font-weight: 500;
  }

  .problem-card__text {
    font-family: var(--font-sans);
    font-size: var(--text-base);
    color: var(--color-text);
    line-height: 1.65;
  }

  /* ── STAKES ───────────────────────────────── */
  .stakes {
    background: var(--color-navy);
    padding-block: var(--space-20);
  }

  .stakes__inner {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-16);
    align-items: center;
  }

  .stakes__eyebrow { color: var(--color-gold) !important; }

  .stakes__heading {
    font-family: var(--font-serif);
    font-size: clamp(var(--text-xl), 2.8vw, var(--text-2xl));
    font-weight: 400;
    color: var(--color-white);
    line-height: 1.25;
    margin-top: var(--space-4);
    margin-bottom: var(--space-6);
  }

  .stakes__sub {
    font-size: var(--text-base);
    color: rgba(255,255,255,0.55);
    line-height: 1.7;
  }

  .stakes__list {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .stakes__item {
    display: flex;
    align-items: flex-start;
    gap: var(--space-3);
    font-size: var(--text-base);
    color: rgba(255,255,255,0.75);
    line-height: 1.5;
    padding-bottom: var(--space-4);
    border-bottom: 0.5px solid rgba(229,224,214,0.1);
  }

  .stakes__item:last-child { border-bottom: none; padding-bottom: 0; }

  .stakes__item svg { color: var(--color-gold); flex-shrink: 0; margin-top: 2px; }

  /* ── GUIDE ────────────────────────────────── */
  .guide__inner {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-20);
    align-items: start;
  }

  .guide__text {
    position: sticky;
    top: calc(64px + var(--space-8));
  }

  .guide__text p {
    font-size: var(--text-base);
    color: var(--color-muted);
    line-height: 1.7;
    margin-bottom: var(--space-4);
  }

  .guide__text .section-title em {
    font-style: italic;
    color: var(--color-terra);
  }

  .guide__btn { margin-top: var(--space-4); }

  .guide__credentials {
    display: flex;
    flex-direction: column;
    gap: var(--space-1);
  }

  .credential {
    display: flex;
    gap: var(--space-6);
    align-items: flex-start;
    padding: var(--space-6);
    border: 0.5px solid transparent;
    border-radius: var(--radius-md);
    background: var(--color-white);
    box-shadow: var(--shadow-card);
    transition: border-color var(--transition-base), transform var(--transition-base), box-shadow var(--transition-base);
  }

  .credential:hover { border-color: var(--color-gold); transform: translateY(-3px); box-shadow: var(--shadow-card-hover); }

  .credential__num {
    font-family: var(--font-serif);
    font-size: var(--text-2xl);
    color: var(--color-gold);
    line-height: 1;
    flex-shrink: 0;
    width: 64px;
  }

  .credential__title {
    display: block;
    font-size: var(--text-sm);
    font-weight: 500;
    color: var(--color-navy);
    letter-spacing: 0.02em;
    margin-bottom: var(--space-1);
  }

  .credential__desc {
    font-size: var(--text-sm);
    color: var(--color-muted);
    line-height: 1.6;
  }

  /* ── PLAN ─────────────────────────────────── */
  .plan { background: var(--color-beige); }

  .plan__steps {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 0;
    position: relative;
  }

  .plan__step {
    padding: var(--space-10) var(--space-8);
    position: relative;
    background: var(--color-white);
    border: var(--border-thin);
    border-right: none;
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .plan__step:last-child { border-right: var(--border-thin); }

  .plan__step-num {
    font-family: var(--font-serif);
    font-size: var(--text-3xl);
    color: var(--color-gold);
    opacity: 0.4;
    line-height: 1;
  }

  .plan__connector {
    position: absolute;
    top: 50%;
    right: -16px;
    width: 32px;
    height: 32px;
    background: var(--color-gold);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 1;
  }

  .plan__connector::after {
    content: '→';
    color: var(--color-white);
    font-size: 14px;
    line-height: 1;
  }

  .plan__step-title {
    font-family: var(--font-serif);
    font-size: var(--text-lg);
    font-weight: 400;
    color: var(--color-navy);
  }

  .plan__step-desc {
    font-size: var(--text-base);
    color: var(--color-muted);
    line-height: 1.65;
  }

  .plan__cta { text-align: center; margin-top: var(--space-12); }

  /* ── SERVICES ─────────────────────────────── */
  .section-header { max-width: 560px; margin-bottom: var(--space-16); }

  .section-header--center { max-width: 560px; margin-inline: auto; margin-bottom: var(--space-16); text-align: center; }

  .section-title {
    font-family: var(--font-serif);
    font-size: clamp(var(--text-2xl), 3.5vw, var(--text-3xl));
    font-weight: 400;
    color: var(--color-navy);
    line-height: 1.2;
    letter-spacing: -0.01em;
  }

  .section-desc { margin-top: var(--space-4); font-size: var(--text-base); color: var(--color-muted); line-height: 1.7; max-width: 480px; }

  .services__grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-6);
  }

  .service-card {
    border: 0.5px solid transparent;
    border-radius: var(--radius-md);
    padding: var(--space-8) var(--space-6);
    background: var(--color-white);
    box-shadow: var(--shadow-card);
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
    transition: transform var(--transition-base), box-shadow var(--transition-base), border-color var(--transition-base);
  }

  .service-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-card-hover);
    border-color: var(--color-gold);
  }

  .service-card__icon {
    color: var(--color-gold);
    width: 48px;
    height: 48px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  .service-card__title {
    font-family: var(--font-serif);
    font-size: var(--text-lg);
    font-weight: 400;
    color: var(--color-navy);
  }

  .service-card__desc {
    font-size: var(--text-sm);
    color: var(--color-muted);
    line-height: 1.65;
    flex: 1;
  }

  .service-card__tag {
    font-size: var(--text-xs);
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--color-gold);
    background: var(--color-gold-20);
    padding: var(--space-1) var(--space-2);
    border-radius: var(--radius-sm);
    align-self: flex-start;
  }

  .services__more { text-align: center; margin-top: var(--space-12); }

  /* ── SUCCESS ──────────────────────────────── */
  .success {
    background: var(--color-navy);
    padding-block: var(--space-24);
  }

  .success__inner { max-width: 900px; margin-inline: auto; }

  .success__media { display: flex; justify-content: center; max-width: 760px; margin: 0 auto var(--space-16); }

  .success__header { text-align: center; margin-bottom: var(--space-12); }

  .success__eyebrow { color: var(--color-gold) !important; display: block; margin-bottom: var(--space-4); }

  .success__heading {
    font-family: var(--font-serif);
    font-size: clamp(var(--text-2xl), 3.5vw, var(--text-3xl));
    font-weight: 400;
    color: var(--color-white);
    line-height: 1.2;
    margin-bottom: var(--space-6);
  }

  .success__heading em { color: var(--color-gold); font-style: italic; }

  .success__sub {
    font-size: var(--text-base);
    color: rgba(255,255,255,0.55);
    max-width: 520px;
    margin-inline: auto;
    line-height: 1.65;
  }

  .success__grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-4) var(--space-10);
  }

  .success__item {
    display: flex;
    align-items: flex-start;
    gap: var(--space-3);
    padding-block: var(--space-4);
    border-bottom: 0.5px solid rgba(229,224,214,0.1);
    color: rgba(255,255,255,0.8);
    font-size: var(--text-base);
    line-height: 1.55;
  }

  .success__check {
    width: 28px;
    height: 28px;
    border-radius: 50%;
    background: rgba(200,154,60,0.15);
    color: var(--color-gold);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  /* ── BENEFICIOS ───────────────────────────── */
  .benefits__grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-6);
  }

  .benefit {
    display: flex;
    gap: var(--space-4);
    align-items: flex-start;
    padding: var(--space-6);
    border: 0.5px solid transparent;
    border-radius: var(--radius-md);
    background: var(--color-white);
    box-shadow: var(--shadow-card);
    transition: border-color var(--transition-base), box-shadow var(--transition-base), transform var(--transition-base);
  }

  .benefit:hover {
    border-color: var(--color-gold);
    box-shadow: var(--shadow-card-hover);
    transform: translateY(-3px);
  }

  .benefit__icon {
    width: 36px;
    height: 36px;
    border-radius: 50%;
    background: var(--color-gold-20);
    color: var(--color-gold);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  .benefit__title {
    font-family: var(--font-serif);
    font-size: var(--text-md);
    font-weight: 400;
    color: var(--color-navy);
    margin-bottom: var(--space-2);
  }

  .benefit__desc {
    font-size: var(--text-sm);
    color: var(--color-muted);
    line-height: 1.6;
  }

  /* ── TESTIMONIOS ──────────────────────────── */
  .testimonials__grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-5);
  }

  .tcard {
    border: 0.5px solid transparent;
    border-radius: var(--radius-md);
    padding: var(--space-7);
    background: var(--color-white);
    box-shadow: var(--shadow-card);
    display: flex;
    flex-direction: column;
    gap: var(--space-5);
    transition: transform var(--transition-base), box-shadow var(--transition-base);
  }

  .tcard:hover { transform: translateY(-3px); box-shadow: var(--shadow-card-hover); }

  .tcard--featured {
    grid-column: span 2;
    background: var(--color-navy);
    border-color: rgba(200,154,60,0.2);
  }

  .tcard__mark { color: var(--color-gold); opacity: 0.3; }

  .tcard__text {
    font-family: var(--font-sans);
    font-size: var(--text-base);
    line-height: 1.75;
    color: var(--color-text);
    flex: 1;
  }

  .tcard--featured .tcard__text { color: rgba(255,255,255,0.85); font-size: var(--text-md); }

  .tcard__footer {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    border-top: var(--border-thin);
    padding-top: var(--space-5);
  }

  .tcard--featured .tcard__footer { border-top-color: rgba(229,224,214,0.15); }

  .tcard__avatar {
    width: 36px;
    height: 36px;
    border-radius: 50%;
    background: var(--color-gold-20);
    color: var(--color-gold);
    font-family: var(--font-serif);
    font-size: var(--text-md);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  .tcard--featured .tcard__avatar { background: rgba(200,154,60,0.15); }

  .tcard__name {
    display: block;
    font-family: var(--font-sans);
    font-size: var(--text-sm);
    font-weight: 500;
    font-style: normal;
    color: var(--color-navy);
  }

  .tcard--featured .tcard__name { color: var(--color-white); }

  .tcard__role { display: block; font-size: var(--text-xs); color: var(--color-muted); }

  .tcard--featured .tcard__role { color: rgba(255,255,255,0.4); }

  /* ── CTA FINAL ────────────────────────────── */
  .cta-final {
    background: var(--color-navy);
    padding-block: var(--space-24);
  }

  .cta-final__inner { max-width: 640px; }

  .cta-final__heading {
    font-family: var(--font-serif);
    font-size: clamp(var(--text-2xl), 3.5vw, var(--text-3xl));
    font-weight: 400;
    color: var(--color-white);
    line-height: 1.2;
    margin-bottom: var(--space-5);
  }

  .cta-final__sub {
    font-size: var(--text-base);
    color: rgba(255,255,255,0.55);
    line-height: 1.7;
    margin-bottom: var(--space-8);
  }

  .cta-final__actions { display: flex; gap: var(--space-4); flex-wrap: wrap; }

  .btn--wa {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    font-family: var(--font-sans);
    font-size: var(--text-xs);
    font-weight: 500;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    text-decoration: none;
    border-radius: var(--radius-sm);
    padding: 14px 24px;
    cursor: pointer;
    background: transparent;
    color: var(--color-white);
    border: 0.5px solid rgba(255,255,255,0.3);
    transition: transform var(--transition-fast), background var(--transition-fast);
  }

  .btn--wa:hover { background: rgba(255,255,255,0.08); }
  .btn--wa:active { transform: scale(0.97); }

  /* ── RESPONSIVE ───────────────────────────── */
  @media (max-width: 1024px) {
    .services__grid { grid-template-columns: repeat(2, 1fr); }
    .guide__inner { grid-template-columns: 1fr; gap: var(--space-12); }
    .guide__text { position: static; }
    .benefits__grid { grid-template-columns: repeat(2, 1fr); }
    .stakes__inner { grid-template-columns: 1fr; gap: var(--space-12); }
    .testimonials__grid { grid-template-columns: 1fr; }
    .tcard--featured { grid-column: span 1; }
    .plan__steps { grid-template-columns: 1fr; }
    .plan__step { border-right: var(--border-thin); border-bottom: none; }
    .plan__step:last-child { border-bottom: var(--border-thin); }
    .plan__connector { top: auto; right: auto; bottom: -16px; left: 50%; transform: translateX(-50%); }
    .plan__connector::after { content: '↓'; }
    .success__grid { grid-template-columns: 1fr; }
    .problem__grid { grid-template-columns: 1fr; }
  }

  @media (max-width: 768px) {
    .hero__inner { grid-template-columns: 1fr; }
    .hero__media { display: none; }
    .stats__inner { display: grid; grid-template-columns: 1fr 1fr; gap: var(--space-8); }
    .stats__divider { display: none; }
    .services__grid { grid-template-columns: 1fr; }
    .benefits__grid { grid-template-columns: 1fr; }
  }

  /* ════ TEMA CLARO — fondo blanco · navy = texto · oro = adorno ════ */
  .hero__inner { align-items: center; }
  .hero__media { width: 320px; }

  .stats, .stakes, .success, .cta-final, .sectors, .plan { background: var(--color-white); }

  .stats__label { color: var(--color-muted); }
  .stats__divider { background: rgba(26, 43, 74, 0.12); }

  .stakes__heading, .success__heading, .cta-final__heading { color: var(--color-navy); }
  .stakes__sub, .success__sub, .cta-final__sub { color: var(--color-muted); }
  .stakes__item, .success__item { color: var(--color-text); border-bottom-color: rgba(26, 43, 74, 0.10); }

  .cta-final .btn--ghost-dark, .cta-final .btn--wa { color: var(--color-navy); border-color: rgba(26, 43, 74, 0.25); }
  .cta-final .btn--ghost-dark:hover, .cta-final .btn--wa:hover { background: var(--color-beige); }

  /* ── GUIDE MEDIA ──────────────────────────── */
  .guide__media { margin-block: var(--space-8); }
</style>
