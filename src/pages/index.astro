---
import Base from '../layouts/Base.astro';

const videoUrl = 'https://www.instagram.com/grupocastanedaoc/reel/DY5fvdYjuWf/';
const sectors = [
  { label: 'Comercio y retail', icon: 'retail' },
  { label: 'Servicios', icon: 'services' },
  { label: 'Logística', icon: 'logistics' },
  { label: 'Construcción', icon: 'construction' },
  { label: 'Salud', icon: 'health' },
  { label: 'Tecnología', icon: 'technology' },
  { label: 'Importación', icon: 'import' },
  { label: 'Consultoría', icon: 'consulting' },
];
const marqueeSectors = [...sectors, ...sectors];
---

<Base
  title="Grupo Castañeda — Outsourcing Contable Integral en Lima"
  description="Contabilidad, planillas, tesorería y tributación integradas. Tu empresa siempre al día, con información real para tomar decisiones. Lima, Perú."
>

  <!-- ══════════════════════════════════════════
       1. HERO — Qué hacemos, para quién, CTA
  ══════════════════════════════════════════ -->
  <section class="hero">
    <img
      class="hero__bg"
      src="/images/contador-oficina-lima.png"
      alt=""
      aria-hidden="true"
      width="1120"
      height="1408"
      loading="eager"
      decoding="async"
    />
    <div class="hero__overlay" aria-hidden="true"></div>

    <div class="container hero__inner">
      <div class="hero__content">
        <span class="eyebrow" data-reveal>Outsourcing Contable Integral · Lima, Perú</span>

        <h1 class="hero__heading" data-reveal>
          Tu empresa crece,<br>
          Deja tu <span class="hero__rotator" aria-hidden="true"><span class="hero__rotator-list"><span class="hero__rotator-word">contabilidad</span><span class="hero__rotator-word">planilla</span><span class="hero__rotator-word">tributación</span><span class="hero__rotator-word">contabilidad</span></span></span><span class="hero__sr-only"> contabilidad, planilla y tributación</span> en manos de expertos
        </h1>

        <p class="hero__sub" data-reveal>
          Mientras tú haces crecer tu negocio, nosotros llevamos tu contabilidad,
          planillas y tributación como un área completa que se anticipa a los
          problemas y te entrega información real para decidir.
        </p>

        <div class="hero__actions" data-reveal>
          <a href="/conversemos" class="btn btn--primary">
            Conversemos
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true">
              <path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </a>
          <a href="/servicios" class="btn btn--outline hero__btn-outline">Ver servicios</a>
        </div>
      </div>
    </div>

    <div class="hero__stats-strip" aria-label="Cifras clave">
      <div class="container hero__stats-inner" data-stagger>
        <div class="hero__stat">
          <span class="hero__stat-num" data-counter="10" data-suffix="+">0</span>
          <span class="hero__stat-label">Años de experiencia</span>
        </div>
        <div class="hero__stat-divider" aria-hidden="true"></div>
        <div class="hero__stat">
          <span class="hero__stat-num" data-counter="150" data-suffix="+">0</span>
          <span class="hero__stat-label">Empresas atendidas</span>
        </div>
        <div class="hero__stat-divider" aria-hidden="true"></div>
        <div class="hero__stat">
          <span class="hero__stat-num" data-counter="4">0</span>
          <span class="hero__stat-label">Áreas integradas</span>
        </div>
        <div class="hero__stat-divider" aria-hidden="true"></div>
        <div class="hero__stat">
          <span class="hero__stat-num" data-counter="98" data-suffix="%">0</span>
          <span class="hero__stat-label">Satisfacción de clientes</span>
        </div>
      </div>
    </div>
  </section>

  <!-- ══════════════════════════════════════════
       2a. VIDEO — Un servicio integral de calidad
  ══════════════════════════════════════════ -->
  <section class="videoint" aria-labelledby="videoint-heading">
    <div class="container">
      <div class="videoint__head" data-reveal>
        <span class="eyebrow">Conócenos</span>
        <h2 class="videoint__title" id="videoint-heading">Un servicio integral de calidad</h2>
      </div>
      <div class="videoint__media" data-reveal>
        <a
          class="video-card"
          href={videoUrl}
          target="_blank"
          rel="noopener noreferrer"
          aria-label="Ver video institucional de Grupo Castañeda"
        >
          <img
            src="/images/diagnostico-contable-dashboard.png"
            alt="Asesor contable presentando indicadores financieros durante una reunión ejecutiva"
            class="video-card__image"
            width="1672"
            height="941"
            loading="lazy"
            decoding="async"
          />
          <span class="video-card__overlay" aria-hidden="true"></span>
          <span class="videoint__play" aria-hidden="true">
            <svg width="26" height="26" viewBox="0 0 24 24" fill="none"><path d="M8 5v14l11-7z" fill="currentColor"/></svg>
          </span>
          <span class="video-card__label">Video — Servicios contables</span>
          <span class="video-card__hint">Ver video institucional</span>
        </a>
      </div>
    </div>
  </section>

  <!-- ══════════════════════════════════════════
       2b. SECTORES — Prueba social: a quiénes atendemos
  ══════════════════════════════════════════ -->
  <section class="sectors" aria-labelledby="sectors-heading">
    <div class="container">

      <div class="sectors__header" data-reveal>
        <span class="eyebrow">Empresas que confían</span>
        <h2 class="sectors__title" id="sectors-heading">
          Acompañamos a empresas en todos los rubros del Perú y del mundo
        </h2>
      </div>

      <div class="rubros-marquee" aria-label="Rubros en los que trabajamos" data-reveal>
        <div class="rubros-marquee__track" aria-hidden="true">
          {marqueeSectors.map(({ label, icon }) => (
            <div class="sector sector--marquee">
              <span class="sector__icon" aria-hidden="true">
                {icon === 'retail' && (
                  <svg width="24" height="24" viewBox="0 0 24 24" fill="none">
                    <path d="M6 7h12l1 13H5L6 7z" stroke="currentColor" stroke-width="1.2" stroke-linejoin="round"/>
                    <path d="M9 9V6a3 3 0 016 0v3" stroke="currentColor" stroke-width="1.2" stroke-linecap="round"/>
                  </svg>
                )}
                {icon === 'services' && (
                  <svg width="24" height="24" viewBox="0 0 24 24" fill="none">
                    <circle cx="12" cy="12" r="3.2" stroke="currentColor" stroke-width="1.2"/>
                    <path d="M12 3v3M12 18v3M3 12h3M18 12h3M5.5 5.5l2 2M16.5 16.5l2 2M18.5 5.5l-2 2M7.5 16.5l-2 2" stroke="currentColor" stroke-width="1.2" stroke-linecap="round"/>
                  </svg>
                )}
                {icon === 'logistics' && (
                  <svg width="24" height="24" viewBox="0 0 24 24" fill="none">
                    <path d="M2 6h11v10H2zM13 9h4l3 3.5V16h-7z" stroke="currentColor" stroke-width="1.2" stroke-linejoin="round"/>
                    <circle cx="6.5" cy="18" r="1.6" stroke="currentColor" stroke-width="1.2"/>
                    <circle cx="16.5" cy="18" r="1.6" stroke="currentColor" stroke-width="1.2"/>
                  </svg>
                )}
                {icon === 'construction' && (
                  <svg width="24" height="24" viewBox="0 0 24 24" fill="none">
                    <rect x="4" y="3" width="16" height="18" rx="1" stroke="currentColor" stroke-width="1.2"/>
                    <path d="M9 21v-4h6v4M8 7h2M14 7h2M8 11h2M14 11h2" stroke="currentColor" stroke-width="1.2" stroke-linecap="round"/>
                  </svg>
                )}
                {icon === 'health' && (
                  <svg width="24" height="24" viewBox="0 0 24 24" fill="none">
                    <path d="M12 21S4 14.5 4 9a4 4 0 018-1 4 4 0 018 1c0 5.5-8 12-8 12z" stroke="currentColor" stroke-width="1.2" stroke-linejoin="round"/>
                  </svg>
                )}
                {icon === 'technology' && (
                  <svg width="24" height="24" viewBox="0 0 24 24" fill="none">
                    <rect x="3" y="4" width="18" height="12" rx="1" stroke="currentColor" stroke-width="1.2"/>
                    <path d="M9 20h6M12 16v4" stroke="currentColor" stroke-width="1.2" stroke-linecap="round"/>
                  </svg>
                )}
                {icon === 'import' && (
                  <svg width="24" height="24" viewBox="0 0 24 24" fill="none">
                    <circle cx="12" cy="12" r="9" stroke="currentColor" stroke-width="1.2"/>
                    <path d="M3 12h18M12 3c3 3 3 15 0 18M12 3c-3 3-3 15 0 18" stroke="currentColor" stroke-width="1.2"/>
                  </svg>
                )}
                {icon === 'consulting' && (
                  <svg width="24" height="24" viewBox="0 0 24 24" fill="none">
                    <circle cx="9" cy="8" r="3" stroke="currentColor" stroke-width="1.2"/>
                    <path d="M3 20c0-3.3 2.7-6 6-6s6 2.7 6 6" stroke="currentColor" stroke-width="1.2" stroke-linecap="round"/>
                    <path d="M16 5.2a3 3 0 010 5.6M21 20c0-2.5-1.5-4.6-3.7-5.5" stroke="currentColor" stroke-width="1.2" stroke-linecap="round"/>
                  </svg>
                )}
              </span>
              <span class="sector__label">{label}</span>
            </div>
          ))}
        </div>
      </div>

    </div>
  </section>

  <!-- ══════════════════════════════════════════
       3. EL PROBLEMA — Resonancia con el cliente
  ══════════════════════════════════════════ -->
  <section class="section problem" aria-labelledby="problem-heading">
    <div class="container">

      <div class="section-header" data-reveal>
        <span class="eyebrow">¿Te identificas?</span>
        <h2 class="section-title" id="problem-heading">
          La contabilidad tradicional<br>ya no es suficiente.
        </h2>
        <div class="gold-line"></div>
      </div>

      <div class="problem__grid" data-stagger>

        <div class="problem-card">
          <div class="problem-card__icon" aria-hidden="true">
            <svg width="28" height="28" viewBox="0 0 28 28" fill="none">
              <circle cx="14" cy="14" r="12" stroke="currentColor" stroke-width="1.2"/>
              <path d="M14 8v7" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
              <circle cx="14" cy="19" r="1" fill="currentColor"/>
            </svg>
          </div>
          <h3 class="problem-card__label">No es solo declarar a fin de mes</h3>
          <p class="problem-card__text">
            Solo recibes declaraciones una vez al mes. Nunca un análisis,
            nunca una alerta, nunca un "ojo con esto".
          </p>
        </div>

        <div class="problem-card problem-card--center">
          <div class="problem-card__icon" aria-hidden="true">
            <svg width="28" height="28" viewBox="0 0 28 28" fill="none">
              <path d="M14 3L3 9v7c0 6.5 4.8 11.1 11 12.3C20.2 27.1 25 22.5 25 16V9L14 3z" stroke="currentColor" stroke-width="1.2"/>
              <path d="M10 14l3 3 6-6" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <h3 class="problem-card__label">Un verdadero control</h3>
          <p class="problem-card__text">
            No sabes realmente si tu empresa está bien. Tomas decisiones
            de inversión, contratación o crédito sin información confiable.
          </p>
        </div>

        <div class="problem-card">
          <div class="problem-card__icon" aria-hidden="true">
            <svg width="28" height="28" viewBox="0 0 28 28" fill="none">
              <path d="M14 3C8 3 3 8 3 14s5 11 11 11 11-5 11-11S20 3 14 3z" stroke="currentColor" stroke-width="1.2"/>
              <path d="M10 10c0-2.2 1.8-4 4-4s4 1.8 4 4c0 2-1.5 3.5-3.5 4v2" stroke="currentColor" stroke-width="1.2" stroke-linecap="round"/>
              <circle cx="14.5" cy="22" r="1" fill="currentColor"/>
            </svg>
          </div>
          <h3 class="problem-card__label">El aliado para el éxito</h3>
          <p class="problem-card__text">
            Mereces un equipo contable que trabaje para tu empresa,
            no solo para cumplir con SUNAT.
          </p>
        </div>

      </div>
    </div>
  </section>

  <!-- ══════════════════════════════════════════
       4. LAS CONSECUENCIAS — Las apuestas
  ══════════════════════════════════════════ -->
  <section class="stakes" aria-labelledby="stakes-heading">
    <div class="container stakes__inner">

      <div class="stakes__text" data-reveal-left>
        <span class="eyebrow stakes__eyebrow">Lo que está en juego</span>
        <h2 class="stakes__heading" id="stakes-heading">
          Cada mes sin control real<br>es un mes de riesgo acumulado.
        </h2>
        <p class="stakes__sub">
          No actuar tiene un costo concreto. La mayoría de los problemas
          contables que terminan en multas o contingencias eran previsibles.
        </p>
      </div>

      <div class="stakes__list" data-stagger>
        <div class="stakes__item">
          <svg width="18" height="18" viewBox="0 0 18 18" fill="none" aria-hidden="true">
            <path d="M9 2L2 5.5v4C2 13.1 5.1 16.4 9 17.2c3.9-.8 7-4.1 7-7.7v-4L9 2z" stroke="currentColor" stroke-width="1.2"/>
            <path d="M6 9l2 2 4-4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
          <span>Multas por declaraciones fuera de plazo o incorrectas</span>
        </div>
        <div class="stakes__item">
          <svg width="18" height="18" viewBox="0 0 18 18" fill="none" aria-hidden="true">
            <path d="M9 2L2 5.5v4C2 13.1 5.1 16.4 9 17.2c3.9-.8 7-4.1 7-7.7v-4L9 2z" stroke="currentColor" stroke-width="1.2"/>
            <path d="M6 9l2 2 4-4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
          <span>Crisis de liquidez que llegan sin aviso</span>
        </div>
        <div class="stakes__item">
          <svg width="18" height="18" viewBox="0 0 18 18" fill="none" aria-hidden="true">
            <path d="M9 2L2 5.5v4C2 13.1 5.1 16.4 9 17.2c3.9-.8 7-4.1 7-7.7v-4L9 2z" stroke="currentColor" stroke-width="1.2"/>
            <path d="M6 9l2 2 4-4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
          <span>Decisiones de inversión sin respaldo financiero real</span>
        </div>
        <div class="stakes__item">
          <svg width="18" height="18" viewBox="0 0 18 18" fill="none" aria-hidden="true">
            <path d="M9 2L2 5.5v4C2 13.1 5.1 16.4 9 17.2c3.9-.8 7-4.1 7-7.7v-4L9 2z" stroke="currentColor" stroke-width="1.2"/>
            <path d="M6 9l2 2 4-4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
          <span>Contingencias tributarias que se acumulan silenciosamente</span>
        </div>
        <div class="stakes__item">
          <svg width="18" height="18" viewBox="0 0 18 18" fill="none" aria-hidden="true">
            <path d="M9 2L2 5.5v4C2 13.1 5.1 16.4 9 17.2c3.9-.8 7-4.1 7-7.7v-4L9 2z" stroke="currentColor" stroke-width="1.2"/>
            <path d="M6 9l2 2 4-4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
          <span>Pérdida de tiempo gerencial resolviendo problemas contables</span>
        </div>
        <div class="stakes__item">
          <svg width="18" height="18" viewBox="0 0 18 18" fill="none" aria-hidden="true">
            <path d="M9 2L2 5.5v4C2 13.1 5.1 16.4 9 17.2c3.9-.8 7-4.1 7-7.7v-4L9 2z" stroke="currentColor" stroke-width="1.2"/>
            <path d="M6 9l2 2 4-4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
          <span>Crecer sin saber si el crecimiento es rentable</span>
        </div>
      </div>

    </div>
  </section>

  <!-- ══════════════════════════════════════════
       5. EL GUÍA — Somos la solución (empatía + autoridad)
  ══════════════════════════════════════════ -->
  <section class="section guide" aria-labelledby="guide-heading">
    <div class="container guide__inner">

      <div class="guide__text" data-reveal-left>
        <span class="eyebrow">Por qué Grupo Castañeda</span>
        <h2 class="section-title" id="guide-heading">
          Entendemos lo que sientes.<br>
          <em>Y sabemos cómo resolverlo.</em>
        </h2>
        <div class="gold-line"></div>
      </div>

      <div class="guide__credentials" data-stagger>

        <div class="credential">
          <span class="credential__num" data-counter="10" data-suffix="+">10+</span>
          <div>
            <strong class="credential__title">Años de experiencia</strong>
            <p class="credential__desc">Más de una década acompañando a empresas desde su organización inicial hasta etapas de consolidación y crecimiento sostenido.</p>
          </div>
        </div>

        <div class="credential">
          <span class="credential__num" data-counter="150" data-suffix="+">150+</span>
          <div>
            <strong class="credential__title">Empresas atendidas</strong>
            <p class="credential__desc">Experiencia con compañías de sectores como comercio, servicios, logística, tecnología, construcción e importación.</p>
          </div>
        </div>

        <div class="credential">
          <span class="credential__num">4</span>
          <div>
            <strong class="credential__title">Áreas integradas</strong>
            <p class="credential__desc">Gestión coordinada de contabilidad, planillas, tesorería y tributación para mantener una operación financiera ordenada.</p>
          </div>
        </div>

      </div>

    </div>

    <div class="container guide__media-panel" data-reveal>
      <figure class="guide__photo">
        <img
          src="/images/equipo-grupo-castaneda-reunion.png"
          alt="Equipo de Grupo Castañeda revisando documentación contable en una reunión de trabajo"
          class="guide__photo-img"
          width="1726"
          height="911"
          loading="lazy"
          decoding="async"
        />
      </figure>
      <a href="/nosotros" class="btn btn--outline guide__btn">Conoce nuestro equipo</a>
    </div>
  </section>

  <!-- ══════════════════════════════════════════
       6. EL PLAN — 3 pasos simples
  ══════════════════════════════════════════ -->
  <section class="section plan" aria-labelledby="plan-heading">
    <div class="container">

      <div class="section-header section-header--center" data-reveal>
        <span class="eyebrow">Así de simple</span>
        <h2 class="section-title" id="plan-heading">
          Tres pasos para tener<br>el área contable que mereces.
        </h2>
        <div class="gold-line" style="margin-inline: auto;"></div>
      </div>

      <div class="plan__steps" data-stagger>

        <div class="plan__step">
          <div class="plan__step-num" aria-hidden="true">01</div>
          <div class="plan__connector" aria-hidden="true"></div>
          <h3 class="plan__step-title">Conversamos</h3>
          <p class="plan__step-desc">
            Una llamada de 30 minutos para entender tu empresa, tu situación
            actual y lo que necesitas. Sin compromiso, sin tecnicismos.
          </p>
        </div>

        <div class="plan__step">
          <div class="plan__step-num" aria-hidden="true">02</div>
          <div class="plan__connector" aria-hidden="true"></div>
          <h3 class="plan__step-title">Diseñamos tu esquema</h3>
          <p class="plan__step-desc">
            Definimos las áreas que necesitas, hacemos la transición ordenada
            desde tu contador o estudio anterior, y ponemos todo al día.
          </p>
        </div>

        <div class="plan__step">
          <div class="plan__step-num" aria-hidden="true">03</div>
          <h3 class="plan__step-title">Tu empresa, siempre al día</h3>
          <p class="plan__step-desc">
            A partir de ese momento tienes un equipo dedicado, alertas preventivas,
            reportes mensuales y respuesta garantizada en el día.
          </p>
        </div>

      </div>

      <div class="plan__cta" data-reveal>
        <a href="/conversemos" class="btn btn--primary">
          Empezar ahora
          <svg width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true">
            <path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </a>
      </div>

    </div>
  </section>

  <!-- ══════════════════════════════════════════
       7. SERVICIOS — Preview de las 4 áreas
  ══════════════════════════════════════════ -->
  <section class="section services" aria-labelledby="services-heading">
    <div class="container">

      <div class="section-header" data-reveal>
        <span class="eyebrow">Lo que hacemos</span>
        <h2 class="section-title" id="services-heading">Cuatro áreas. Un solo equipo.</h2>
        <div class="gold-line"></div>
        <p class="section-desc">
          Cada área trabaja en coordinación para darte una visión financiera
          completa y preventiva de tu empresa.
        </p>
      </div>

      <div class="services__grid" data-stagger>

        <div class="service-card">
          <div class="service-card__icon" aria-hidden="true">
            <svg width="28" height="28" viewBox="0 0 28 28" fill="none">
              <rect x="4" y="3" width="20" height="23" rx="2" stroke="currentColor" stroke-width="1.2"/>
              <line x1="8" y1="10" x2="20" y2="10" stroke="currentColor" stroke-width="1.2"/>
              <line x1="8" y1="15" x2="20" y2="15" stroke="currentColor" stroke-width="1.2"/>
              <line x1="8" y1="20" x2="15" y2="20" stroke="currentColor" stroke-width="1.2"/>
            </svg>
          </div>
          <h3 class="service-card__title">Contabilidad</h3>
          <p class="service-card__desc">Registro riguroso, estados financieros reales y análisis mensual que te permite decidir con datos.</p>
          <span class="service-card__tag">NIIF / PCGE</span>
        </div>

        <div class="service-card">
          <div class="service-card__icon" aria-hidden="true">
            <svg width="28" height="28" viewBox="0 0 28 28" fill="none">
              <circle cx="14" cy="9" r="5" stroke="currentColor" stroke-width="1.2"/>
              <path d="M5 25c0-4.97 4.03-9 9-9s9 4.03 9 9" stroke="currentColor" stroke-width="1.2" stroke-linecap="round"/>
            </svg>
          </div>
          <h3 class="service-card__title">Planillas</h3>
          <p class="service-card__desc">Cálculo de remuneraciones, AFP/ONP, gratificaciones, CTS y presentación puntual ante SUNAT.</p>
          <span class="service-card__tag">T-Registro / PLM</span>
        </div>

        <div class="service-card">
          <div class="service-card__icon" aria-hidden="true">
            <svg width="28" height="28" viewBox="0 0 28 28" fill="none">
              <rect x="3" y="7" width="22" height="16" rx="2" stroke="currentColor" stroke-width="1.2"/>
              <path d="M3 12h22" stroke="currentColor" stroke-width="1.2"/>
              <circle cx="9" cy="18" r="2" stroke="currentColor" stroke-width="1.2"/>
            </svg>
          </div>
          <h3 class="service-card__title">Tesorería</h3>
          <p class="service-card__desc">Control de flujo de caja, conciliaciones bancarias y alertas preventivas de liquidez.</p>
          <span class="service-card__tag">Flujo de caja</span>
        </div>

        <div class="service-card">
          <div class="service-card__icon" aria-hidden="true">
            <svg width="28" height="28" viewBox="0 0 28 28" fill="none">
              <path d="M14 3L3 8.5v5.5c0 6.4 4.8 11.4 11 12.7C20.2 25.4 25 20.4 25 14v-5.5L14 3z" stroke="currentColor" stroke-width="1.2"/>
              <path d="M10 14l3 3 6-6" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <h3 class="service-card__title">Tributación</h3>
          <p class="service-card__desc">Planificación tributaria, declaraciones, atención de fiscalizaciones y estrategias para reducir la carga fiscal.</p>
          <span class="service-card__tag">SUNAT / PDT</span>
        </div>

      </div>

      <div class="services__more" data-reveal>
        <a href="/servicios" class="btn btn--outline">Ver detalle de servicios</a>
      </div>

    </div>
  </section>

  <!-- ══════════════════════════════════════════
       8. BENEFICIOS — Value stack
  ══════════════════════════════════════════ -->
  <section class="section benefits" aria-labelledby="benefits-heading">
    <div class="container">

      <div class="section-header" data-reveal>
        <span class="eyebrow">Lo que incluye</span>
        <h2 class="section-title" id="benefits-heading">
          Todo lo que necesita<br>tu empresa, integrado.
        </h2>
        <div class="gold-line"></div>
      </div>

      <div class="benefits__grid" data-stagger>

        <div class="benefit">
          <div class="benefit__icon" aria-hidden="true">
            <svg width="22" height="22" viewBox="0 0 22 22" fill="none">
              <path d="M4 11l4 4 10-8" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <div>
            <h3 class="benefit__title">Equipo dedicado a tu empresa</h3>
            <p class="benefit__desc">Un contador principal asignado, con nombre y apellido. Siempre sabes a quién escribirle.</p>
          </div>
        </div>

        <div class="benefit">
          <div class="benefit__icon" aria-hidden="true">
            <svg width="22" height="22" viewBox="0 0 22 22" fill="none">
              <path d="M4 11l4 4 10-8" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <div>
            <h3 class="benefit__title">Respuesta garantizada en el día</h3>
            <p class="benefit__desc">Ninguna consulta queda sin respuesta por más de 24 horas hábiles.</p>
          </div>
        </div>

        <div class="benefit">
          <div class="benefit__icon" aria-hidden="true">
            <svg width="22" height="22" viewBox="0 0 22 22" fill="none">
              <path d="M4 11l4 4 10-8" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <div>
            <h3 class="benefit__title">Alertas preventivas</h3>
            <p class="benefit__desc">Te avisamos antes de vencimientos, contingencias y riesgos de liquidez.</p>
          </div>
        </div>

        <div class="benefit">
          <div class="benefit__icon" aria-hidden="true">
            <svg width="22" height="22" viewBox="0 0 22 22" fill="none">
              <path d="M4 11l4 4 10-8" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <div>
            <h3 class="benefit__title">Reportes ejecutivos mensuales</h3>
            <p class="benefit__desc">En lenguaje de negocio, no tecnicismos: estado de resultados, flujo y KPIs clave.</p>
          </div>
        </div>

        <div class="benefit">
          <div class="benefit__icon" aria-hidden="true">
            <svg width="22" height="22" viewBox="0 0 22 22" fill="none">
              <path d="M4 11l4 4 10-8" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <div>
            <h3 class="benefit__title">Fee fijo sin sorpresas</h3>
            <p class="benefit__desc">Un precio mensual que incluye todo. Sin cobros extra por declaraciones adicionales.</p>
          </div>
        </div>

        <div class="benefit">
          <div class="benefit__icon" aria-hidden="true">
            <svg width="22" height="22" viewBox="0 0 22 22" fill="none">
              <path d="M4 11l4 4 10-8" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <div>
            <h3 class="benefit__title">Transición sin fricciones</h3>
            <p class="benefit__desc">Nos encargamos de ordenar el histórico si vienes de otro estudio o contador.</p>
          </div>
        </div>

      </div>
    </div>
  </section>

  <!-- ══════════════════════════════════════════
       9. CTA FINAL
  ══════════════════════════════════════════ -->
  <section class="cta-final" aria-labelledby="cta-final-heading">
    <div class="container cta-final__inner" data-reveal>
      <div>
        <h2 class="cta-final__heading" id="cta-final-heading">
          Todo inicia por<br>un diagnóstico.
        </h2>
        <p class="cta-final__sub">
          Cuéntanos sobre tu empresa por WhatsApp. Te respondemos antes de las 24 horas
          y te orientamos con el siguiente paso.
        </p>
        <div class="cta-final__actions">
          <a
            href="https://wa.me/51969357784?text=Hola%2C%20me%20gustar%C3%ADa%20conocer%20m%C3%A1s%20sobre%20sus%20servicios%20contables."
            class="btn btn--wa"
            target="_blank"
            rel="noopener noreferrer"
          >
            <svg width="18" height="18" viewBox="0 0 18 18" fill="none" aria-hidden="true">
              <path d="M9 1.5C4.86 1.5 1.5 4.86 1.5 9c0 1.41.38 2.74 1.04 3.89L1.5 16.5l3.74-1.01A7.47 7.47 0 009 16.5c4.14 0 7.5-3.36 7.5-7.5S13.14 1.5 9 1.5z" stroke="currentColor" stroke-width="1.2"/>
              <path d="M6.5 7.5c.5 1.2 1.5 2.8 3.5 3.5" stroke="currentColor" stroke-width="1.2" stroke-linecap="round"/>
            </svg>
            WhatsApp
          </a>
        </div>
      </div>
    </div>
  </section>

</Base>

<style>
  /* ── HERO ─────────────────────────────────── */
  .hero {
    background: var(--color-navy);
    padding-top: calc(108px + var(--space-24));
    position: relative;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    min-height: 100vh;
  }

  .hero__bg {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: right center;
    z-index: 0;
  }

  .hero__overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      to right,
      rgba(26, 43, 74, 0.97) 0%,
      rgba(26, 43, 74, 0.90) 30%,
      rgba(26, 43, 74, 0.62) 55%,
      rgba(26, 43, 74, 0.22) 75%,
      rgba(26, 43, 74, 0.06) 100%
    );
    z-index: 1;
  }

  .hero__inner {
    flex: 1;
    display: flex;
    align-items: center;
    padding-bottom: var(--space-20);
    position: relative;
    z-index: 2;
  }

  .hero__content { max-width: 600px; }

  .hero__content .eyebrow { color: var(--color-gold); }

  .hero__heading {
    font-family: var(--font-serif);
    font-size: clamp(var(--text-3xl), 5.5vw, var(--text-5xl));
    font-weight: 400;
    color: var(--color-white);
    line-height: 1.1;
    letter-spacing: -0.01em;
    margin-top: var(--space-4);
    margin-bottom: var(--space-6);
  }

  .hero__heading em { font-style: italic; color: var(--color-gold); }

  /* ── Carrusel vertical de palabras (contabilidad / planilla / tributación) ── */
  .hero__rotator {
    display: inline-block;
    height: 1.15em;
    line-height: 1.15;
    overflow: hidden;
    vertical-align: bottom;
    white-space: nowrap;
    width: 5.35em;
    animation: hero-rotate-w 9s cubic-bezier(0.76, 0, 0.24, 1) infinite;
  }

  .hero__rotator-list {
    display: block;
    animation: hero-rotate-y 9s cubic-bezier(0.76, 0, 0.24, 1) infinite;
  }

  .hero__rotator-word {
    display: block;
    height: 1.15em;
    line-height: 1.15;
    font-style: italic;
    color: var(--color-gold);
  }

  /* Desplazamiento vertical de la palabra */
  @keyframes hero-rotate-y {
    0%, 21%    { transform: translateY(0); }
    25%, 46%   { transform: translateY(-1.15em); }
    50%, 71%   { transform: translateY(-2.30em); }
    75%, 100%  { transform: translateY(-3.45em); }
  }

  /* Ancho del contenedor en sync — el texto siguiente se ajusta a cada palabra */
  @keyframes hero-rotate-w {
    0%, 21%    { width: 5.35em; }  /* contabilidad */
    25%, 46%   { width: 3.35em; }  /* planilla */
    50%, 71%   { width: 4.81em; }  /* tributación */
    75%, 100%  { width: 5.35em; }  /* contabilidad */
  }

  .hero__sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  @media (prefers-reduced-motion: reduce) {
    .hero__rotator-list,
    .hero__rotator { animation: none; }
  }

  .hero__sub {
    font-size: var(--text-base);
    line-height: 1.7;
    color: rgba(255,255,255,0.65);
    max-width: 520px;
    margin-bottom: var(--space-10);
  }

  .hero__actions { display: flex; gap: var(--space-4); flex-wrap: wrap; }

  .hero__btn-outline {
    background: transparent;
    color: var(--color-white);
    border-color: rgba(255,255,255,0.25);
  }

  .hero__btn-outline:hover {
    background: rgba(255,255,255,0.06);
    border-color: rgba(255,255,255,0.45);
  }

  /* ── Brillo dorado girando alrededor de "Conversemos" (igual que el nav) ── */
  .hero__actions .btn--primary {
    position: relative;
    isolation: isolate;
  }

  @property --hero-cta-glow {
    syntax: '<angle>';
    initial-value: 0deg;
    inherits: false;
  }

  .hero__actions .btn--primary::before {
    content: '';
    position: absolute;
    inset: -2px;
    border-radius: calc(var(--radius-sm) + 2px);
    padding: 1.5px;
    background: conic-gradient(
      from var(--hero-cta-glow),
      transparent 0deg,
      rgba(200, 154, 60, 0.12) 40deg,
      var(--color-gold) 72deg,
      #fff4d6 88deg,
      var(--color-gold) 104deg,
      transparent 150deg,
      transparent 360deg
    );
    -webkit-mask: linear-gradient(#000 0 0) content-box, linear-gradient(#000 0 0);
            mask: linear-gradient(#000 0 0) content-box, linear-gradient(#000 0 0);
    -webkit-mask-composite: xor;
            mask-composite: exclude;
    filter: drop-shadow(0 0 4px rgba(200, 154, 60, 0.6));
    animation: hero-cta-orbit 2.8s linear infinite;
    z-index: -1;
    pointer-events: none;
  }

  @keyframes hero-cta-orbit { to { --hero-cta-glow: 360deg; } }

  @media (prefers-reduced-motion: reduce) {
    .hero__actions .btn--primary::before { animation: none; }
  }

  .hero__stats-strip {
    position: relative;
    z-index: 2;
    border-top: 0.5px solid rgba(255, 255, 255, 0.08);
    background: rgba(26, 43, 74, 0.72);
    backdrop-filter: blur(14px);
    -webkit-backdrop-filter: blur(14px);
    padding-block: var(--space-5);
  }

  .hero__stats-inner {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-12);
  }

  .hero__stat {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-2);
    text-align: center;
  }

  .hero__stat-num {
    font-family: var(--font-serif);
    font-size: var(--text-3xl);
    font-weight: 400;
    color: var(--color-gold);
    line-height: 1;
  }

  .hero__stat-label {
    font-size: var(--text-xs);
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.4);
    max-width: 120px;
    text-align: center;
  }

  .hero__stat-divider {
    width: 0.5px;
    height: 48px;
    background: rgba(229, 224, 214, 0.15);
    flex-shrink: 0;
  }

  /* ── STATS ────────────────────────────────── */
  .stats { background: var(--color-navy); padding-block: var(--space-12); }

  .stats__inner {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-12);
  }

  .stats__item { display: flex; flex-direction: column; align-items: center; gap: var(--space-2); text-align: center; }

  .stats__num {
    font-family: var(--font-serif);
    font-size: var(--text-3xl);
    font-weight: 400;
    color: var(--color-gold);
    line-height: 1;
  }

  .stats__label {
    font-size: var(--text-xs);
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: rgba(255,255,255,0.4);
    max-width: 120px;
    text-align: center;
  }

  .stats__divider { width: 0.5px; height: 48px; background: rgba(229,224,214,0.15); flex-shrink: 0; }

  /* ── SECTORES ─────────────────────────────── */
  .sectors {
    background: var(--color-white);
    padding-block: var(--space-24);
    border-bottom: var(--border-thin);
  }

  .sectors__header {
    text-align: center;
    max-width: 760px;
    margin: 0 auto var(--space-16);
  }

  .sectors__title {
    font-family: var(--font-serif);
    font-size: clamp(var(--text-2xl), 3.5vw, var(--text-3xl));
    font-weight: 400;
    color: var(--color-navy);
    line-height: 1.16;
    margin-top: var(--space-3);
  }

  .sector {
    display: flex;
    align-items: center;
    gap: var(--space-4);
    min-width: 260px;
    padding: var(--space-6) var(--space-7);
    border: var(--border-thin);
    border-radius: var(--radius-md);
    background: var(--color-white);
    box-shadow: var(--shadow-card);
    transition: border-color var(--transition-base), transform var(--transition-base), box-shadow var(--transition-base);
  }

  .sector:hover {
    border-color: var(--color-gold);
    transform: translateY(-2px);
    box-shadow: var(--shadow-card-hover);
  }

  .sector__icon {
    width: 48px;
    height: 48px;
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-gold);
    background: var(--color-gold-20);
    border-radius: var(--radius-sm);
  }

  .sector__label {
    font-size: var(--text-md);
    color: var(--color-navy);
    letter-spacing: 0.01em;
  }

  /* ── VIDEO INTEGRAL ───────────────────────── */
  .videoint { padding-block: var(--space-24); background: var(--color-white); }

  .videoint__head { text-align: center; max-width: 640px; margin: 0 auto var(--space-12); }

  .videoint__title {
    font-family: var(--font-serif);
    font-size: clamp(var(--text-2xl), 3.5vw, var(--text-3xl));
    font-weight: 400;
    color: var(--color-navy);
    line-height: 1.2;
    margin-top: var(--space-3);
  }

  .videoint__media { max-width: 880px; margin: 0 auto; }

  .video-card {
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    width: 100%;
    aspect-ratio: 16 / 9;
    padding: var(--space-8);
    overflow: hidden;
    text-align: center;
    text-decoration: none;
    color: var(--color-navy);
    background:
      radial-gradient(circle at 50% 35%, rgba(200, 154, 60, 0.16), transparent 34%),
      linear-gradient(135deg, #ffffff, var(--color-soft));
    border: var(--border-thin);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-card);
    transition: transform var(--transition-base), box-shadow var(--transition-base), border-color var(--transition-base);
  }

  .video-card:hover {
    transform: translateY(-3px);
    border-color: rgba(200,154,60,0.55);
    box-shadow: var(--shadow-card-hover);
  }

  .videoint__play {
    width: 64px;
    height: 64px;
    border-radius: 50%;
    background: var(--color-gold);
    color: var(--color-white);
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: var(--space-3);
    position: relative;
    z-index: 1;
    box-shadow: 0 16px 36px rgba(0,0,0,0.24);
  }

  .video-card__image,
  .video-card__overlay {
    position: absolute;
    inset: 0;
  }

  .video-card__image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: 52% 46%;
    transition: transform var(--transition-base);
  }

  .video-card__overlay {
    background: linear-gradient(180deg, rgba(26,43,74,0.16), rgba(26,43,74,0.68));
    z-index: 0;
  }

  .video-card:hover .video-card__image { transform: scale(1.035); }

  .video-card__label {
    font-size: var(--text-sm);
    font-weight: 500;
    color: var(--color-white);
    letter-spacing: 0.03em;
    position: relative;
    z-index: 1;
  }

  .video-card__hint {
    margin-top: var(--space-2);
    font-size: var(--text-xs);
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: rgba(255,255,255,0.72);
    position: relative;
    z-index: 1;
  }

  /* ── RUBROS ROTATIVOS ───────────── */
  .rubros-marquee {
    overflow: hidden;
    -webkit-mask-image: linear-gradient(90deg, transparent, #000 8%, #000 92%, transparent);
    mask-image: linear-gradient(90deg, transparent, #000 8%, #000 92%, transparent);
  }

  .rubros-marquee__track {
    display: flex;
    gap: var(--space-6);
    width: max-content;
    animation: rubros-marquee 34s linear infinite;
  }

  .sector--marquee {
    flex-shrink: 0;
  }

  @keyframes rubros-marquee {
    from { transform: translateX(0); }
    to   { transform: translateX(-50%); }
  }

  @media (prefers-reduced-motion: reduce) {
    .rubros-marquee__track { animation: none; }
  }

  /* ── PROBLEMA ─────────────────────────────── */
  .problem {
    background: var(--color-white);
    border-top: var(--border-thin);
  }

  .problem .section-header {
    text-align: center;
    margin-inline: auto;
  }

  .problem .gold-line {
    margin-inline: auto;
  }

  .problem__grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-6);
    align-items: stretch;
  }

  .problem-card {
    min-height: 100%;
    border: var(--border-thin);
    border-radius: var(--radius-md);
    padding: var(--space-8) var(--space-7);
    background: var(--color-white);
    box-shadow: var(--shadow-card);
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
    text-align: left;
  }

  .problem-card--center {
    background: var(--color-navy);
    border-color: rgba(200,154,60,0.3);
  }

  .problem-card--center .problem-card__icon { color: var(--color-gold); }
  .problem-card--center .problem-card__label { color: var(--color-gold); }
  .problem-card--center .problem-card__text { color: rgba(255,255,255,0.75); }

  .problem-card__icon {
    color: var(--color-terra);
    width: 48px;
    height: 48px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(184, 92, 58, 0.08);
    border-radius: var(--radius-sm);
  }

  .problem-card__label {
    font-size: var(--text-xs);
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--color-terra);
    font-weight: 500;
  }

  .problem-card__text {
    font-family: var(--font-sans);
    font-size: var(--text-base);
    color: var(--color-text);
    line-height: 1.65;
  }

  /* ── STAKES ───────────────────────────────── */
  .stakes {
    background: var(--color-white);
    padding-block: var(--space-24);
    border-top: var(--border-thin);
  }

  .stakes__inner {
    display: grid;
    grid-template-columns: minmax(0, 0.95fr) minmax(0, 1.05fr);
    gap: var(--space-12);
    align-items: stretch;
  }

  .stakes__text,
  .stakes__list {
    border: var(--border-thin);
    border-radius: var(--radius-md);
    background: var(--color-white);
    box-shadow: var(--shadow-card);
  }

  .stakes__text {
    padding: var(--space-10);
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .stakes__eyebrow { color: var(--color-gold) !important; }

  .stakes__heading {
    font-family: var(--font-serif);
    font-size: clamp(var(--text-xl), 2.8vw, var(--text-2xl));
    font-weight: 400;
    color: var(--color-navy);
    line-height: 1.25;
    margin-top: var(--space-4);
    margin-bottom: var(--space-6);
  }

  .stakes__sub {
    font-size: var(--text-base);
    color: var(--color-muted);
    line-height: 1.7;
  }

  .stakes__list {
    display: grid;
    grid-template-columns: 1fr;
    padding: var(--space-6);
    gap: 0;
  }

  .stakes__item {
    display: flex;
    align-items: flex-start;
    gap: var(--space-3);
    font-size: var(--text-base);
    color: var(--color-text);
    line-height: 1.5;
    padding: var(--space-4);
    border-bottom: var(--border-thin);
  }

  .stakes__item:last-child { border-bottom: none; padding-bottom: 0; }

  .stakes__item svg { color: var(--color-gold); flex-shrink: 0; margin-top: 2px; }

  /* ── GUIDE ────────────────────────────────── */
  .guide {
    background: var(--color-white);
    border-top: var(--border-thin);
  }

  .guide__inner {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: var(--space-10);
    align-items: stretch;
  }

  .guide__text {
    display: flex;
    flex-direction: column;
    justify-content: center;
    border: var(--border-thin);
    border-radius: var(--radius-md);
    background: var(--color-white);
    box-shadow: var(--shadow-card);
    padding: var(--space-10);
  }

  .guide__text p {
    font-size: var(--text-base);
    color: var(--color-muted);
    line-height: 1.7;
    margin-bottom: var(--space-4);
  }

  .guide__text .section-title em {
    font-style: italic;
    color: var(--color-terra);
  }

  .guide__media-panel {
    margin-top: var(--space-12);
    text-align: center;
  }

  .guide__photo {
    width: 100%;
    aspect-ratio: 1726 / 911;
    margin: 0;
    border-radius: var(--radius-md);
    box-shadow: var(--shadow-card);
    overflow: hidden;
    border: var(--border-thin);
    background: var(--color-white);
  }

  .guide__photo-img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: 50% 50%;
  }

  .guide__btn {
    margin-top: var(--space-6);
    justify-content: center;
  }

  .guide__credentials {
    display: grid;
    grid-template-columns: 1fr;
    grid-template-rows: repeat(3, 1fr);
    gap: var(--space-5);
  }

  .credential {
    display: flex;
    gap: var(--space-6);
    align-items: center;
    padding: var(--space-6);
    min-height: 0;
    border: var(--border-thin);
    border-radius: var(--radius-md);
    background: var(--color-white);
    box-shadow: var(--shadow-card);
    transition: border-color var(--transition-base), transform var(--transition-base), box-shadow var(--transition-base);
  }

  .credential:hover { border-color: var(--color-gold); transform: translateY(-3px); box-shadow: var(--shadow-card-hover); }

  .credential__num {
    font-family: var(--font-serif);
    font-size: var(--text-2xl);
    color: var(--color-gold);
    line-height: 1;
    flex-shrink: 0;
    width: 64px;
  }

  .credential__title {
    display: block;
    font-size: var(--text-sm);
    font-weight: 500;
    color: var(--color-navy);
    letter-spacing: 0.02em;
    margin-bottom: var(--space-1);
  }

  .credential__desc {
    font-size: var(--text-sm);
    color: var(--color-muted);
    line-height: 1.6;
  }

  /* ── PLAN ─────────────────────────────────── */
  .plan {
    background: var(--color-white);
    border-top: var(--border-thin);
    border-bottom: var(--border-thin);
  }

  .plan__steps {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-6);
    position: relative;
  }

  .plan__step {
    min-height: 260px;
    padding: var(--space-10) var(--space-8);
    position: relative;
    background: var(--color-white);
    border: var(--border-thin);
    border-radius: var(--radius-md);
    box-shadow: var(--shadow-card);
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .plan__step-num {
    font-family: var(--font-serif);
    font-size: var(--text-3xl);
    color: var(--color-gold);
    opacity: 0.4;
    line-height: 1;
  }

  .plan__connector {
    position: absolute;
    top: 50%;
    right: calc(-1 * var(--space-6));
    width: 32px;
    height: 32px;
    background: var(--color-gold);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 1;
  }

  .plan__connector::after {
    content: '→';
    color: var(--color-white);
    font-size: 14px;
    line-height: 1;
  }

  .plan__step-title {
    font-family: var(--font-serif);
    font-size: var(--text-lg);
    font-weight: 400;
    color: var(--color-navy);
  }

  .plan__step-desc {
    font-size: var(--text-base);
    color: var(--color-muted);
    line-height: 1.65;
  }

  .plan__cta { text-align: center; margin-top: var(--space-12); }

  /* ── SERVICES ─────────────────────────────── */
  .section-header { max-width: 560px; margin-bottom: var(--space-16); }

  .section-header--center { max-width: 560px; margin-inline: auto; margin-bottom: var(--space-16); text-align: center; }

  .section-title {
    font-family: var(--font-serif);
    font-size: clamp(var(--text-2xl), 3.5vw, var(--text-3xl));
    font-weight: 400;
    color: var(--color-navy);
    line-height: 1.2;
    letter-spacing: -0.01em;
  }

  .section-desc { margin-top: var(--space-4); font-size: var(--text-base); color: var(--color-muted); line-height: 1.7; max-width: 480px; }

  .services__grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-6);
  }

  .service-card {
    border: 0.5px solid transparent;
    border-radius: var(--radius-md);
    padding: var(--space-8) var(--space-6);
    background: var(--color-white);
    box-shadow: var(--shadow-card);
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
    transition: transform var(--transition-base), box-shadow var(--transition-base), border-color var(--transition-base);
  }

  .service-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-card-hover);
    border-color: var(--color-gold);
  }

  .service-card__icon {
    color: var(--color-gold);
    width: 48px;
    height: 48px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  .service-card__title {
    font-family: var(--font-serif);
    font-size: var(--text-lg);
    font-weight: 400;
    color: var(--color-navy);
  }

  .service-card__desc {
    font-size: var(--text-sm);
    color: var(--color-muted);
    line-height: 1.65;
    flex: 1;
  }

  .service-card__tag {
    font-size: var(--text-xs);
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--color-gold);
    background: var(--color-gold-20);
    padding: var(--space-1) var(--space-2);
    border-radius: var(--radius-sm);
    align-self: flex-start;
  }

  .services__more { text-align: center; margin-top: var(--space-12); }

  /* ── BENEFICIOS ───────────────────────────── */
  .benefits__grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-6);
  }

  .benefit {
    display: flex;
    gap: var(--space-4);
    align-items: flex-start;
    padding: var(--space-6);
    border: 0.5px solid transparent;
    border-radius: var(--radius-md);
    background: var(--color-white);
    box-shadow: var(--shadow-card);
    transition: border-color var(--transition-base), box-shadow var(--transition-base), transform var(--transition-base);
  }

  .benefit:hover {
    border-color: var(--color-gold);
    box-shadow: var(--shadow-card-hover);
    transform: translateY(-3px);
  }

  .benefit__icon {
    width: 36px;
    height: 36px;
    border-radius: 50%;
    background: var(--color-gold-20);
    color: var(--color-gold);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  .benefit__title {
    font-family: var(--font-serif);
    font-size: var(--text-md);
    font-weight: 400;
    color: var(--color-navy);
    margin-bottom: var(--space-2);
  }

  .benefit__desc {
    font-size: var(--text-sm);
    color: var(--color-muted);
    line-height: 1.6;
  }

  /* ── CTA FINAL ────────────────────────────── */
  .cta-final {
    background: var(--color-navy);
    padding-block: var(--space-24);
  }

  .cta-final__inner {
    max-width: 700px;
    text-align: center;
  }

  .cta-final__heading {
    font-family: var(--font-serif);
    font-size: clamp(var(--text-2xl), 3.5vw, var(--text-3xl));
    font-weight: 400;
    color: var(--color-white);
    line-height: 1.2;
    margin-bottom: var(--space-5);
  }

  .cta-final__sub {
    font-size: var(--text-base);
    color: rgba(255,255,255,0.55);
    line-height: 1.7;
    margin-bottom: var(--space-8);
  }

  .cta-final__actions {
    display: flex;
    justify-content: center;
    gap: var(--space-4);
    flex-wrap: wrap;
  }

  .btn--wa {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    font-family: var(--font-sans);
    font-size: var(--text-xs);
    font-weight: 500;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    text-decoration: none;
    border-radius: var(--radius-sm);
    padding: 14px 24px;
    cursor: pointer;
    background: transparent;
    color: var(--color-white);
    border: 0.5px solid rgba(255,255,255,0.3);
    transition: transform var(--transition-fast), background var(--transition-fast);
  }

  .btn--wa:hover { background: rgba(255,255,255,0.08); }
  .btn--wa:active { transform: scale(0.97); }

  /* ── RESPONSIVE ───────────────────────────── */
  @media (max-width: 1024px) {
    .services__grid { grid-template-columns: repeat(2, 1fr); }
    .guide__inner { grid-template-columns: 1fr; gap: var(--space-12); }
    .guide__text { position: static; }
    .benefits__grid { grid-template-columns: repeat(2, 1fr); }
    .stakes__inner { grid-template-columns: 1fr; gap: var(--space-12); }
    .plan__steps { grid-template-columns: 1fr; }
    .plan__step { min-height: auto; }
    .plan__connector { top: auto; right: auto; bottom: -16px; left: 50%; transform: translateX(-50%); }
    .plan__connector::after { content: '↓'; }
    .problem__grid { grid-template-columns: 1fr; }
  }

  @media (max-width: 768px) {
    .hero__stats-inner { display: grid; grid-template-columns: 1fr 1fr; gap: var(--space-8); }
    .hero__stat-divider { display: none; }
    .services__grid { grid-template-columns: 1fr; }
    .benefits__grid { grid-template-columns: 1fr; }
  }

  /* ════ TEMA CLARO — fondo blanco · navy = texto · oro = adorno ════ */
  .hero__inner { align-items: center; }

  .stats, .stakes, .cta-final, .sectors, .plan { background: var(--color-white); }

  .stats__label { color: var(--color-muted); }
  .stats__divider { background: rgba(26, 43, 74, 0.12); }

  .stakes__heading, .cta-final__heading { color: var(--color-navy); }
  .stakes__sub, .cta-final__sub { color: var(--color-muted); }
  .stakes__item { color: var(--color-text); border-bottom-color: rgba(26, 43, 74, 0.10); }

  .cta-final .btn--ghost-dark, .cta-final .btn--wa { color: var(--color-navy); border-color: rgba(26, 43, 74, 0.25); }
  .cta-final .btn--ghost-dark:hover, .cta-final .btn--wa:hover { background: var(--color-soft); }

  /* ── GUIDE MEDIA ──────────────────────────── */
</style>
